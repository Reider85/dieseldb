3.0.87 Prompt 45 - JSONL nested storage modes (flatten/json_column) and array handling (json/expand), SQL JSON Path support

- diesel/storage/json/JsonParserConfig.java: new NestedMode (FLATTEN|JSON_COLUMN, default FLATTEN -> jsonl.nested.mode) and ArrayColumnsMode (JSON|EXPAND, default JSON -> jsonl.array.columns) hooks; sysprop -> config.properties -> default resolution with invalid-override fallback; builder setters, equals/hashCode/toString, javadoc
- diesel/storage/json/JsonPathResolver.java (new, public): longest-prefix dot-path resolution to ResolvedPath(columnIndex, column, segments) with case-insensitive matching; resolveQualified drops one leading TABLE. segment then retries so USERS.user.address.city finds a user.address.city column; token-level extract (streaming parser, no DOM)
- diesel/storage/JsonlRowReader.java: FLATTEN mode walks nested objects into exact dot-notation leaf columns (user.address.city) via containerPrefixes + walkObjectFlat with the same validation/conversion as top-level fields; scalar arrays expand to field[0..N-1] index columns under expand (arrays of objects fall back to a whole JSON column); object leaves without child columns captured as compact JSON text and marked as nested holders; nextProjected in flatten parses the full row then projects (arrays must be walked to reach index columns)
- diesel/storage/JsonlRowWriter.java: FLATTEN mode reconstructs real nested JSON from dot-columns + index columns in deterministic schema-column order; nested-JSON holder cells (marked on read) re-embedded as structure so load->save never double-encodes strings; json_column mode writes holders as structure
- diesel/storage/JsonlSchemaManager.java: nestedJsonColumns holder set + markNestedJson; validateFlattenSchema rejects a schema mixing column X with a dot-path row X.y
- diesel/storage/JsonlRowStorage.java: one shared JsonlSchemaManager kept across the load->save lifecycle (refreshed on load plan, reset on failed-load rollback) so nested-holder marks survive into saveToFile
- diesel/storage/json/JsonSchemaInference.java: dot-notation leaf and index columns pass through inference unchanged
- diesel/SqlParsingUtils.java: normalizeColumnName keeps dotted identifiers whole when the first segment is not a table alias (case-insensitive alias resolution); unquoteQualifiedIdentifier unquotes whole quoted identifiers then each dot part; validateColumn + resolveColumnType accept JSON Path prefixes; QueryParser.resolveType and SubqueryParser.getColumnType delegate to resolveColumnType
- diesel/SelectQuery.java: resolveRowValue + pathResolutionCache route WHERE (scalar/IN/IS NULL/column-comparison) and projection fallback through JsonPathResolver for json_column mode; computeNormalizeColumnName resolves alias prefixes case-insensitively
- config.properties: jsonl.nested.mode = flatten, jsonl.array.columns = json documented defaults
- pom.xml + src/test/java/diesel/JsonlNestedModeTest.java (new, 10 tests) registered in default + ci surefire
- Verification: new class isolated 10/10 green; quick suite (mvn test -DskipLargeTests) 370/0/0/6; full suite (4GB heap, @LargeTest) 370/0/0/0 BUILD SUCCESS; timing gate: timing/timing125.md vs baseline timing/timing.md -> 0 regressions (108 improvements, 28 unchanged), exit 0. Profile check skipped - prompt 45 has no JOIN / hash join / performance wording (strict condition); make/changelog targets absent on this machine so the changelog entry was appended manually.