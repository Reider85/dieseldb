2.9.95 Fix the 7 Parquet-mode test failures (ORDER BY / GROUP BY / lazy delete / persistence): with storage.format=PARQUET, 7 tests failed because (1) the columnar auto-switch in SelectQuery.executeSelect() projected ONLY the SELECT columns from Parquet, so ORDER BY / GROUP BY / aggregate-argument columns outside the projection (e.g. SELECT NAME FROM SMOKE ORDER BY AGE) were dropped from the read rows and sorting/aggregation evaluated null keys -> SELECT NAME FROM SMOKE ORDER BY AGE DESC returned [Alice,Bob] and SUM(AGE) in aggregatesAreComputedPerGroup returned null; (2) saveToParquetFile() ran compact() whenever getDeletedCount()>0, so every auto-commit DELETE (persisted through saveToFile -> saveToParquetFile) physically removed the tombstones and getDeletedCount() reported 0 in LazyDeleteTest.rowCountAfterDelete and manualCompactRebuildsIndexes; (3) PersistenceTest hard-coded .csv existence assertions that cannot hold when the configured format is PARQUET. Changes: diesel/SelectQuery.java - collectProjectedColumnNames(mainTableName, table) now additionally projects the ORDER BY, GROUP BY, aggregate-argument and WHERE-condition columns (filtered against the real table columns, deduped via LinkedHashSet, helper addProjectedColumn/addProjectedConditionColumns); diesel/Table.java - removed the if(getDeletedCount()>0){compact();} block from saveToParquetFile() so lazy-delete tombstones survive saves exactly like the CSV path (ParquetWriter already skips tombstones via Table.toTableData/buildTableData), compaction remains driven by the 30% threshold in DeleteQuery or explicit Table.compact(); src/test/java/diesel/PersistenceTest.java - testSaveAndLoadRoundTrip and testDropTableDeletesFiles now assert the format-appropriate persistence file (.parquet in PARQUET mode, .csv otherwise) via StorageFormat.formatForTable(TABLE,1). Verification on JDK 21: Parquet mode (config storage.format=PARQUET) DatabaseSmokeTest+DeadCodeRemovalTest+GroupByTest+LazyDeleteTest+PersistenceTest+ColumnarStorageTest 94 tests 0 failures 0 errors BUILD SUCCESS; config restored to CSV default and the same 94 tests pass 0 failures 0 errors (CSV path intact); quick gate mvn test -DskipLargeTests 42 tests 0 failures 0 errors 2 skipped BUILD SUCCESS; full acceptance gate mvn -Ptest -Ddiesel.largeTests=true -Dtest.heap=4g test 790 tests 0 failures 0 errors 0 skipped BUILD SUCCESS; timing vs timing37.md 0 regressions (10 improvements from machine noise, exit 0). Profile check skipped per AGENTS.md step 6 (no JOIN/performance keyword in the task).