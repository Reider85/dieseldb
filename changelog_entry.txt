2.9.91 (post-90 follow-up) RegexPerformanceBenchmarkTest.benchmarkParse10kQueries flake fix: the 10k-query parse benchmark (prompt 27, java:S5842 regression guard) asserted elapsedMs < 30_000, but it takes ~15s in isolation and ~50s when run concurrently with the heavy @LargeTest joins under -Dtest.heap=4g, so the full acceptance gate flaked on a loaded machine. Raised the guard to 60_000 ms and clarified the comment that the test targets a catastrophic-backtracking blow-up (typically 10x-1000x), not the ~2x noise of a loaded concurrent run; a true regex backtracking regression still fails at this bound. Verification on JDK 21: quick gate mvn test -DskipLargeTests 42 tests 0 failures 0 errors 2 skipped; full acceptance gate mvn -Ptest -Ddiesel.largeTests=true -Dtest.heap=4g test 788 tests 0 failures 0 errors 0 skipped BUILD SUCCESS (benchmark ~15s within the 60s bound). Profile check skipped per AGENTS.md step 6 (no JOIN/performance keyword in the task).