2.9.94 Introduce the pluggable storage-format framework (diesel.format) per analytics/refactoring.md phases 1-5:

New diesel.format package: TableFormat interface, FormatRegistry (CSV + PARQUET pre-registered), TableData carrier, FormatCapabilities, ColumnPredicate, ReadOptions, WriteOptions, CompressionCodec, CsvFormat, ParquetFormat.

Table.saveToFile's non-Parquet branch now delegates to CsvFormat via TableFormat.write(TableData,...) with new saveViaFormatHandler/resolveFormat/toTableData/buildTableData helpers; engine metadata travels in the carrier under the TableData.META_* keys. ParquetWriter/ParquetReader refactored to a TableData-driven core (writeTableData/readTableData/buildMetadata from the META_* keys), the Table-based entry points routing through Table.toTableData(), plus a public diesel.FormatSupport facade bridging the package-private Parquet layer to the handlers. StorageFormat rewritten for per-table resolution order storage.format.<TABLE> -> storage.format.default -> storage.format with AUTO mapped post-hoc to Table.COLUMNAR_THRESHOLD_ROWS, and Database persist/load paths made per-table format aware. config.properties default storage.format restored to CSV (also fixes the pre-existing PersistenceTest CSV-vs-PARQUET failures noted in 2.9.93).

Verification on JDK 21: quick gate mvn test -DskipLargeTests green (AllTestsSampleTest 21/0/0/1, QuantitativeTest 21/0/0/1, incl. PersistenceTest 4/0); targeted ParquetReaderTest 22/0/0 with 2 new framework round-trip/config-resolution tests; full acceptance gate mvn -Ddiesel.largeTests=true -Dtest.heap=4g test 42/0/0 BUILD SUCCESS (heavy 600x600 ORDER BY joins pass). Profile check skipped per AGENTS.md step 6 (no JOIN/performance keyword in this refactoring task).