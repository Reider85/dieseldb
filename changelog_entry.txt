3.0.78 Prompt 41 - JSONL storage extended features (type validation, field projection, JSON Path dot-notation, schema sidecar)

- new diesel/storage/JsonlSchemaManager: schema owner with read-side validateReadToken (rejects only silent-corruption shapes - nested object/array into typed columns, number into Boolean/date/UUID - with file:line:field diagnostics) and write-side validateWriteValue (Map/List/array into typed columns, float/BigDecimal into integer columns with record N: field 'X'); JSON Path base (resolveProjectionItem longest-prefix + extractPathValue token-level walk); <name>.schema.json sidecar write/read/verify (SCHEMA_FORMAT_VERSION=1)
- JsonlRowReader: nextArray() validated via the schema; new setProjection/nextProjected/nextProjectedMap/getProjectionItems with token-level skipping of non-requested fields and parsed/skipped counters; projection does not affect full reads; unresolved items dropped with one WARNING
- JsonlRowWriter: new 3-arg ctor with columnTypes wiring write validation (2-arg unchanged, output identical); JsonlRowStorage.saveToFile passes columnTypes (1-line)
- JsonlSchemaProjectionTest (19 tests): read/write validation coordinates, lenient string coercion, projection counters on 40-col rows (parsed=3x, skipped=37x), dot path DATA.user.address.city -> "Moscow", sidecar round-trip + mismatch, storage save-reject keeps previous file, load-reject rolls back
- Benchmark (20k rows x 40 cols): full 620.8ms vs projected 227.2ms (2.7x faster), records parsed=60000/skipped=740000
- Quick 297/0/0, full suite (4GB, @LargeTest) 297/0/0/0 BUILD SUCCESS, timing exit 0 (heavy >=100ms queries stable), profile skipped (no JOIN/performance keywords)