3.0.89 Prompt 47 - JSONL NULL semantics: null vs missing field vs empty string stay distinct across a load->save round trip

- diesel/storage/json/JsonParserConfig.java: new jsonl.missing.field policy (null | error | default; default DEFAULT = missing reads back as null, sysprop -> config.properties -> default, invalid-override fallback) with the MissingFieldMode enum wired into builder/accessor/constructor/equals/hashCode/toString; config.properties documents jsonl.missing.field = default
- diesel/storage/JsonlRowReader.java: per-row field-presence tracking (boolean[] lastRowPresent, exposed via getLastRowPresent(), cloned on read); jsonl.missing.field=error fails the offending row with a DieselIOException carrying file:line and the missing column names (enforceMissingFieldPolicy; the missing-key contract of strict schema mode from prompt 44 may now raise instead of silently nulling); typeAt made null-safe so schemaless loads (null columnTypes) work
- diesel/storage/JsonlRowWriter.java: new writeRow(Object[], boolean[]) overload - a key whose presence flag is false is not written; null stays JSON null, empty string stays "", missing stays missing. Plain writeRow(Object[]) keeps the pre-prompt behaviour (every schema column written), so old callers are byte-identical
- diesel/storage/JsonlRowStorage.java: parallel rowPresence list (boolean[] per row) maintained by insert/insertAt/update/delete/setRows (in-memory rows all-present) and carried through saveToFile -> writer and loadFromFile -> reader presence flags (rollback-safe on failed load); new getRowPresence()/setRowPresence(); javadoc documents the scan() contract - a missing key is surfaced as the null slot plus an absent presence flag rather than being silently collapsed into an explicit null (Query Executor sees unchanged null-at-Map-boundary semantics; no query-engine change needed)
- Backward compatibility: pre-prompt-47 files whose nulls were written as missing keys load all-present (missing -> null by default), and old files with explicit null keys round-trip byte-identically - no migration required
- New src/test/java/diesel/JsonlNullSemanticsTest.java (11 tests): writer omits missing keys but keeps explicit null and empty string; null presence mask is the all-present fallback; reader presence flags per row; error mode throws with file:line + column names; default mode maps missing to null; storage round trip keeps null vs missing distinct; inserted rows all-present; nested object with null inside keeps its key; absent nested leaf stays absent; array with null elements round-trips; legacy file round-trips byte-identically. Registered in pom.xml default + ci surefire includes (the explicit-includes list would otherwise compile but silently skip the class)
- Verification: isolated class 11/11 green; quick suite (mvn test -DskipLargeTests) 381/0/0/6; full suite (4GB heap, @LargeTest) 381/0/0/0 BUILD SUCCESS. Timing vs timing/timing.md baseline (manual heavy-query comparison - the awk script mis-parses the markdown timing rows): the only >=100ms baseline queries are stable (SubqueriesTest 194.3 -> 173.1ms, JoinTest 169.3 -> 13.3ms, both OK); flagged sub-100ms moves are machine noise in both directions. Profile check skipped - prompt 47 has no JOIN / hash join / performance wording (strict condition); make/changelog targets absent on this machine so the changelog entry was appended manually.