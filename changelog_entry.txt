2.9.86 prompt 86 Apache Parquet integration - ParquetWriter (prompt2.md line 1500): added a low-level Apache Parquet writer (parquet-column 1.13.1 + parquet-hadoop 1.13.1 + hadoop-common 3.3.4) that serializes a Table to Parquet format entirely through the ParquetFileWriter(OutputFile, ParquetMetadata, ...) low-level API with a custom FileChannel-backed OutputFile/PositionOutputStream - completely bypassing Hadoop FileSystem (no HADOOP_HOME/winutils.exe dependency, fixes the earlier Hadoop-shell failure on Windows). New diesel/ParquetWriter.java (class-level package-private): buildSchema() maps Table column types to Parquet MessageType (INTEGER/LONG/SHORT/BYTE/INT64, FLOAT/DOUBLE, BIGDECIMAL as UTF8 BINARY via toPlainString, CHAR/UUID/STRING/DATE/DATETIME/DATETIME_MS as UTF8/BINARY/LOGICAL types, BOOLEAN), writeTableToParquet(Table, String) skips tombstoned (deleted) rows via Table.getLiveRows() and writes each row through a GroupWriter using SNAPPY compression via a non-direct CodecFactory (avoids the direct-allocator path that needs winutils), writeValue() handles the type conversion for each column. New src/test/java/diesel/ParquetWriterTest.java (2 tests): writes a file and asserts 'PAR1' magic bytes at both start and end (valid second-generation Parquet footer format), and verifies tombstoned rows are skipped on write. pom.xml: added parquet-column, parquet-hadoop, hadoop-common (hadoop-mapreduce-client-core removed; hadoop-common now marked N/A scope to keep the engine standalone). Verification on JDK 21: mvn compile BUILD SUCCESS; ParquetWriterTest 2/2 BUILD SUCCESS (~19s); quick gate mvn test -DskipLargeTests green - 42 tests 0 failures 0 errors 2 skipped BUILD SUCCESS.
