# Prompts 75 + 76: Java 17/21 compatibility, launch scripts, tests, timing check, commit & push

## Goal
Execute prompt 75 (document Java 17 & 21 LTS compatibility; remove deprecated APIs; verify compilation under both) **and** prompt 76 (create `start-server.sh/.bat` + `start-client.sh/.bat` launch scripts accepting parameters), **sequentially, in two separate commits**. For EACH prompt: run `mvn test` WITHOUT the `-Ptest` profile, verify timings did not degrade >20% (aggregate ≤ 1.2x, 0 FAIL) vs the latest baseline, and on success commit + push to `origin/main`.

User decisions (confirmed):
- Both prompts, separate commits: prompt 75 = version 2.7.59, prompt 76 = version 2.7.60.
- Prompt 76: extend BOTH mains — `DatabaseServer.main` takes port + data dir (data written to that dir), `DatabaseClient.main` takes host + port.

## Current state (verified)
- HEAD = `1fa00b9` (2.7.58 prompt 74). Working tree: only `USERS.csv` (tracked, modified — must NOT be staged) plus untracked artifacts (`run_*.log`, root `timing*.md`, `*.csv`/`*.table`, `.codebuddy/`, `.kilo/`, `src/main/`). No staged changes.
- Only deprecated API in project sources: `BigDecimal.ROUND_HALF_UP` (14 usages): `diesel/SelectQuery.java:428` + `src/test/java/diesel/{AdvancedTest:38, AliasesTest:74,87, AllTestsSampleTest:223,237, GroupByTest:87, InTest:35, JoinTest:68, LikeTest:35, OrderByTest:90, SubqueriesTest:66, QuantitativeTest:167,181}`. No `import java.math.RoundingMode` anywhere yet.
- Engine sources `diesel/*.java`; tests `src/test/java/diesel/*.java`. Default `mvn test` runs only `AllTestsSampleTest` (199/0, writes root `timingN.md`) + `QuantitativeTest` (199/0).
- Tracked timing baseline: `timing/timing56.md` (138 queries, 0 FAIL). Root untracked timing files end at `timing34.md` → next run writes root `timing35.md`, then `timing36.md`.
- Timing compare tool: `"C:\Users\user\AppData\Local\Temp\kilo\compare-timing.ps1" -OldFile <old> -NewFile <new>` (aggregate ratio, degraded>1.2, improved<0.8; exits 1 on row-count mismatch).
- Maven: `"C:\tools\apache-maven-3.9.6\bin\mvn.cmd"` (full path; `mvn` NOT on PATH). Default JDK: AxiomJDK-21 (21.0.11) — used for all baseline timing runs. JDK 17: `C:\Program Files\JetBrains\IntelliJ IDEA Community Edition 2023.3.1\jbr`.
- `config.properties` loaded via classloader from classpath root — launch scripts must include repo root (`.`) in `-cp` so it resolves.
- `Database` writes table files relative to CWD (`.`) via `loadTablesFromDisk()`, `saveTablesToDisk()`, `deleteTableFiles()`; `Table.saveToFile/saveToSerializedFile/loadFromFile` use bare filenames. `Table` holds a transient `Database database` reference → data dir can be obtained via `database.getDataDir()`.
- `DatabaseServer.main`: `args[0]` = port (default 3306). `DatabaseClient.main`: no args (hardcoded localhost:3306).
- `.github/workflows/ci.yml` is a stub (checkout only).

## Phase A — Prompt 75 (2.7.59)

### A1. Remove deprecated API (behavior-preserving)
Replace every `BigDecimal.ROUND_HALF_UP` with `RoundingMode.HALF_UP` (identical semantics, non-deprecated `divide(BigDecimal,int,RoundingMode)` overload) and add `import java.math.RoundingMode;` after `import java.math.BigDecimal;` in each of the 11 files above. Do NOT touch `server.stop()`/`process.destroy()` (not deprecated).

### A2. Documentation
- `README.md`: lines 1/8/15/24/28 ("built on Java 17" / "на Java 17" / "auf Basis von Java 17" / "en Java 17" / "基于 Java 17") → state Java 17 & 21 (LTS) compatibility in all 5 languages; line 42 "JDK 17 or newer" and line 66 "JDK 17 или новее" → "JDK 17 or 21 (LTS)" / "JDK 17 или 21 (LTS)".
- `PHASE0_CHANGES.md`: line 121 README reference → mention JDK 17/21; add section 18 "Java 17 & 21 (LTS) Compatibility" (bytecode 17 via `maven.compiler.release=17`, verified under both JDKs, no deprecated APIs — ROUND_HALF_UP→RoundingMode.HALF_UP); update line 135 version 2.7.57 → 2.7.59.
- `PERSISTENCE_README.md` line 4: "проект заявлен на Java 17" → "проект совместим с Java 17 и 21 (LTS)".
- `Changelog.md`: append one 2.7.59 line (written AFTER timing analysis is known; text = commit message).

### A3. pom.xml
Replace lines 13–14 (`<maven.compiler.source>17` / `<maven.compiler.target>17`) with `<maven.compiler.release>17</maven.compiler.release>`.

### A4. CI matrix (`.github/workflows/ci.yml`)
Keep triggers (push/PR to main, `runs-on: ubuntu-latest`). Add `strategy.matrix.java: [17, 21]`; steps: checkout (`actions/checkout@v4`), setup-java@v4 (`distribution: temurin`, `java-version: ${{ matrix.java }}`, `cache: maven`), then `mvn -B test`. Validate YAML (cannot run CI locally; it mirrors local `mvn test`).

### A5. Deprecation lint (JDK 21, default)
`"C:\tools\apache-maven-3.9.6\bin\mvn.cmd" clean test-compile -Dmaven.compiler.showDeprecation=true -Dmaven.compiler.compilerArgs=-Xlint:deprecation` → BUILD SUCCESS and NO `[WARNING] ... deprecated` lines for project sources.

### A6. JDK 17 compile verification
`$env:JAVA_HOME = "C:\Program Files\JetBrains\IntelliJ IDEA Community Edition 2023.3.1\jbr"` then `"C:\tools\apache-maven-3.9.6\bin\mvn.cmd" clean test-compile` → BUILD SUCCESS. If the permission layer blocks the `$env:JAVA_HOME=...` assignment, fall back to `--release 17` under JDK 21 (equivalent guarantee) and note it. Do NOT generate a timing report under 17.

### A7. Canonical timing run (JDK 21, no -Ptest)
Ensure `$env:JAVA_HOME` = AxiomJDK-21 (default), run `"C:\tools\apache-maven-3.9.6\bin\mvn.cmd" test` with a large timeout (≥ 6 min; suite does 600×4 inserts + join/subquery/server tests), output to `run_prompt75.log`. Expect BUILD SUCCESS, `AllTestsSampleTest results: 199 passed, 0 failed`, `QuantitativeTest ... 199 passed, 0 failed`, root `timing35.md` created.

### A8. Timing comparison
1. `Copy-Item` root `timing35.md` → `timing/timing57.md`.
2. `"C:\Users\user\AppData\Local\Temp\kilo\compare-timing.ps1" -OldFile timing/timing56.md -NewFile timing/timing57.md`.
3. Pass iff 0 FAIL and aggregate ≤ 1.2x. If aggregate > 1.2x: rerun A7 once (writes `timing36.md`), use the rerun as `timing/timing57.md`, record both numbers (established repo precedent). If still > 1.2x → STOP, no commit/push, report to user.

### A9. Commit & push (only if A5–A8 pass)
Stage ONLY via explicit `git add <paths>` (never `git add .`): `README.md`, `pom.xml`, `PHASE0_CHANGES.md`, `Changelog.md`, `diesel/SelectQuery.java`, the 10 test files, `.github/workflows/ci.yml`, `timing/timing57.md`. Commit message: single long descriptive line starting `2.7.59 prompt 75 (Java 17 & 21 (LTS) compatibility): ...` including doc updates, deprecated-API cleanup, `maven.compiler.release=17`, CI matrix, dual-JDK verification, and timing analysis (`timing report timing57.md (138 queries, 0 FAIL; no degradation - aggregate X.XXXx vs timing56 (N degraded / M improved / K stable within the 20% band)...)`). `git push origin main`. No force-push; if rejected → report.

## Phase B — Prompt 76 (2.7.60)

### B1. Database data dir support (engine change — timing check mandatory)
- `diesel/Database.java`: add `private String dataDir = ".";`, constructor `Database(String dataDir)` (keep default no-arg), `public String getDataDir()`, `public void setDataDir(String)`; use `dataDir` in `loadTablesFromDisk()` (line ~343), `saveTablesToDisk()`, `deleteTableFiles()` (prepend `dataDir + File.separator`, create dirs if missing).
- `diesel/Table.java`: `saveToFile`, `saveToSerializedFile`, `loadFromFile` build filenames as `(database != null ? database.getDataDir() + File.separator : "") + tableName + ext` (fallback to CWD when database is null). Behavior-preserving with default `.`.
- `diesel/DatabaseServer.java` `main`: `args[0]` = port (default 3306), `args[1]` = data dir (optional, default "."). Construct `new Database(dataDir)` and pass to `DatabaseServer`. Create the data dir if missing.

### B2. DatabaseClient.main args
`diesel/DatabaseClient.java` `main`: accept `args[0]` = host (default `localhost`), `args[1]` = port (default 3306) instead of hardcoded values; keep existing demo logic.

### B3. Launch scripts (repo root)
Create 4 files:
- `start-server.sh` / `start-server.bat`: params PORT (default 3306), DATA_DIR (default `.`), plus optional JAR/classpath detection. Steps: build classpath (`target/classes` + repo root `.` for `config.properties` + dependency jars resolved via `mvn dependency:build-classpath` into a temp file, or documented fallback), ensure DATA_DIR exists, run `java -cp ... diesel.DatabaseServer <PORT> <DATA_DIR>`. On `.sh`: `#!/usr/bin/env bash` + `set -e`; allow `PORT`/`DATA_DIR` positional or env overrides.
- `start-client.sh` / `start-client.bat`: params HOST (default `localhost`), PORT (default 3306); run `java -cp ... diesel.DatabaseClient <HOST> <PORT>`.
- Windows `.bat` uses `set`/`%1`-style arg handling; `.sh` uses `$1`/`$2` with defaults. Scripts must work from the repo root and tolerate being invoked from other CWDs (resolve script dir).

### B4. Canonical timing run (JDK 21, no -Ptest)
`"C:\tools\apache-maven-3.9.6\bin\mvn.cmd" test` (output `run_prompt76.log`), large timeout. Expect BUILD SUCCESS, 199/0 + 199/0, root `timing36.md` (next free index — adjust if A8 rerun consumed it).

### B5. Timing comparison
Copy root new report → `timing/timing58.md`; compare against `timing/timing57.md`. Pass iff 0 FAIL and aggregate ≤ 1.2x; if > 1.2x, rerun once and use the clean run; if still > 1.2x → STOP, report.

### B6. Changelog
Append one 2.7.60 line (commit message text) after the 2.7.59 line.

### B7. Commit & push
Stage ONLY: `start-server.sh`, `start-server.bat`, `start-client.sh`, `start-client.bat`, `diesel/Database.java`, `diesel/Table.java`, `diesel/DatabaseServer.java`, `diesel/DatabaseClient.java`, `Changelog.md`, `timing/timing58.md`. Commit message: single line starting `2.7.60 prompt 76 launch scripts (start-server/start-client .sh/.bat): ...` incl. main args (server port+data dir, client host+port), timing analysis. `git push origin main`.

## Validation summary
- A5/A6: zero deprecation warnings; compile OK under both JDKs. A7/B4: BUILD SUCCESS, 199/0 + 199/0. A8/B5: aggregate ≤ 1.2x, 0 FAIL. A9/B7: commit contains only intended files (explicit `git add`), push succeeds.
- Both prompts leave working-tree artifacts untracked: never stage `USERS.csv`, root `timing*.md` (originals), `run_*.log`, `*.csv`/`*.table`, `src/main/`, `.codebuddy/`, `.idea/`, `.kilo/`.

## Risks & caveats
- Prompt 76 changes engine files (Database/Table) → timing comparison is mandatory (B4/B5).
- `$env:JAVA_HOME` assignment may be blocked by session permissions; use `--release 17` fallback and report (A6).
- `mvn dependency:build-classpath` may need to run inside B3 — validate the exact command once before finalizing scripts.
- CI matrix cannot be validated locally; only YAML validity + parity with local `mvn test`.
- First timing run may catch machine load (known 0.5x–2.4x noise band on this box) — rerun and use the clean run per established precedent.
