# Prompt 75: Java 17 & 21 (LTS) compatibility

## Goal
Execute prompt 75 from `prompt.md`: (1) document that the project is compatible with Java 17 and 21 (LTS), (2) ensure the code does not use deprecated APIs, (3) verify the code compiles under both versions. Then run tests **without** the `-Ptest` profile, verify timings did not degrade >20% vs the latest baseline, and on success commit & push.

## Key context (verified)
- Last commit `1fa00b9` = "2.7.58 prompt 74..." → this is 2.7.59.
- Engine sources: `diesel/*.java` (not `src/main/java`). Tests: `src/test/java/diesel/*.java`.
- Local JDKs: JDK 21 = `C:\Program Files\Axiom\AxiomJDK-21` (21.0.11 LTS, default `java`, used for ALL baseline timing runs); JDK 17 = IntelliJ JBR `C:\Program Files\JetBrains\IntelliJ IDEA Community Edition 2023.3.1\jbr` (17.0.9, includes javac).
- Maven: `"C:\tools\apache-maven-3.9.6\bin\mvn.cmd"` (invoke without `&` operator; set `$env:JAVA_HOME` before switching JDK).
- Default `mvn test` (no profile) runs only `AllTestsSampleTest` + `QuantitativeTest` (last run: 199/0 each, ~1.5-2 min). AllTestsSampleTest writes the timing report to the repo root as `timingN.md` (root currently has `timing.md`+`timing1..34.md` untracked → next is root `timing35.md`); tracked convention is `timing/timingN.md` (latest = `timing/timing56.md`).
- Timing compare script: `"C:\Users\user\AppData\Local\Temp\kilo\compare-timing.ps1" -OldFile timing/timing56.md -NewFile timing/timing57.md`. It parses `| # |` rows; degraded = ratio > 1.2x, improved = < 0.8x; exits 1 on row-count mismatch.
- Pass criterion (project methodology): 0 FAIL and aggregate ratio <= 1.2x vs the previous run; if a transient heavy-load first run shows aggregate > 1.2x, rerun once and use the rerun (past runs documented and discarded such runs).
- `USERS.csv` is tracked and currently modified — a runtime artifact; must NOT be staged. Many untracked artifacts (`run_*.log`, root `timing*.md`, `*.csv`/`*.table`, `.codebuddy/`, `.kilo/`, `src/main/`) — do NOT stage them.
- Current `.github/workflows/ci.yml` is a stub (checkout only, no JDK setup / test step).
- Project convention: each prompt adds a one-line entry to `Changelog.md` (2.7.5x) and the git commit message is that same long descriptive line including the timing analysis.

## Decisions (confirmed with user)
1. Verification scope: local dual-JDK compilation **plus** extend `ci.yml` with a JDK 17+21 matrix.
2. `pom.xml`: replace `maven.compiler.source/target=17` with `maven.compiler.release=17`.

## Tasks

### T1. Remove deprecated API usage (behavior-preserving)
Replace `BigDecimal.ROUND_HALF_UP` with `RoundingMode.HALF_UP` (identical semantics, non-deprecated overload `divide(BigDecimal, int, RoundingMode)`). Add `import java.math.RoundingMode;` where missing.

- Engine: `diesel/SelectQuery.java:428` (add import at line ~3).
- Tests (11 usages): `src/test/java/diesel/AdvancedTest.java:38`; `AliasesTest.java:74,87`; `AllTestsSampleTest.java:223,237`; `GroupByTest.java:87`; `InTest.java:35`; `JoinTest.java:68`; `LikeTest.java:35`; `OrderByTest.java:90`; `SubqueriesTest.java:66`; `QuantitativeTest.java:167,181`.

Note: `server.stop()` / `process.destroy()` are NOT deprecated — leave untouched.

### T2. Documentation
- `README.md`:
  - Lines 1/8/15/24/28 ("built on Java 17" / "на Java 17" / "auf Basis von Java 17" / "en Java 17" / "基于 Java 17") → state Java 17 & 21 (LTS) compatibility, e.g. "built on Java 17, compatible with Java 17 and 21 (LTS)" (+ RU/DE/ES/ZH equivalents).
  - Line 42 "JDK 17 or newer" and line 66 "JDK 17 или новее" → "JDK 17 or 21 (LTS)" / "JDK 17 или 21 (LTS)".
- `PHASE0_CHANGES.md`:
  - Update line 121 (README reference: "Maven, JDK 17, commands") to mention JDK 17/21.
  - Add section 18 "Java 17 & 21 (LTS) Compatibility": project compiles to Java 17 bytecode (`maven.compiler.release=17`), verified under both JDK 17 and 21, no deprecated APIs used (ROUND_HALF_UP → RoundingMode.HALF_UP).
  - Update line 135 version: 2.7.57 → 2.7.59.
- `Changelog.md`: append a single-line 2.7.59 entry (same text as the commit message; write it AFTER timing analysis is known).

### T3. pom.xml
- Replace lines 13-14 (`<maven.compiler.source>17</...>`, `<maven.compiler.target>17</...>`) with `<maven.compiler.release>17</maven.compiler.release>`.

### T4. CI matrix (`.github/workflows/ci.yml`)
Rewrite (keep triggers on push/PR to main; `runs-on: ubuntu-latest`):
- `strategy.matrix.java: [17, 21]`
- Steps: checkout (actions/checkout@v4), setup-java@v4 with `distribution: temurin`, `java-version: ${{ matrix.java }}`, `cache: maven`, then `mvn -B test`.

### T5. Deprecated-API lint (both JDKs)
Under each JDK:
`"C:\tools\apache-maven-3.9.6\bin\mvn.cmd" clean test-compile -Dmaven.compiler.showDeprecation=true -Dmaven.compiler.compilerArgs=-Xlint:deprecation`
Expect BUILD SUCCESS and NO `[WARNING] ... has been deprecated` lines for project sources.

### T6. JDK 17 compile verification
Set `$env:JAVA_HOME = "C:\Program Files\JetBrains\IntelliJ IDEA Community Edition 2023.3.1\jbr"`, run `"C:\tools\apache-maven-3.9.6\bin\mvn.cmd" clean test-compile` → BUILD SUCCESS. (Compile-only; do NOT generate a timing report under 17.)

### T7. Canonical timing run (JDK 21, no -Ptest)
Set `$env:JAVA_HOME = "C:\Program Files\Axiom\AxiomJDK-21"`, run `"C:\tools\apache-maven-3.9.6\bin\mvn.cmd" test`.
Expect BUILD SUCCESS, AllTestsSampleTest 199/0 + QuantitativeTest 199/0, and a new root `timing35.md`.

### T8. Timing comparison
1. Copy the newly generated root report to `timing/timing57.md` (`Copy-Item`).
2. Run `"C:\Users\user\AppData\Local\Temp\kilo\compare-timing.ps1" -OldFile timing/timing56.md -NewFile timing/timing57.md`.
3. Pass iff 0 FAIL and aggregate <= 1.2x. If aggregate > 1.2x, rerun T7 once; use the rerun's report as `timing/timing57.md` and record both numbers (project precedent). If still > 1.2x, STOP — do not commit/push; report to the user.

### T9. Commit & push (only if T5-T8 pass)
- Stage ONLY: `README.md`, `pom.xml`, `PHASE0_CHANGES.md`, `Changelog.md`, `diesel/SelectQuery.java`, the 10 test files, `.github/workflows/ci.yml`, `timing/timing57.md`. Use explicit `git add` (or `git add <paths>`), never `git add .`.
- Commit message: single long descriptive line following project style, e.g. `2.7.59 prompt 75 (Java 17 & 21 (LTS) compatibility): ...` including: doc updates (README 5 languages, PHASE0_CHANGES, Changelog), deprecated-API cleanup (BigDecimal.ROUND_HALF_UP → RoundingMode.HALF_UP in SelectQuery.java and 11 test usages, identical semantics), maven.compiler.release=17, CI matrix JDK 17/21, dual-JDK compile verification results, and the timing analysis: `timing report timing57.md (138 queries, 0 FAIL; no degradation - aggregate X.XXXx vs timing56 (... degraded / ... improved / ... stable within the 20% band)...)`.
- Push: `git push`. Do NOT force-push; if push is rejected (non-fast-forward), report to the user.

## Validation summary
- Zero deprecation warnings under both JDKs (T5); BUILD SUCCESS for JDK 17 test-compile (T6) and JDK 21 full test (T7); timing aggregate <= 1.2x with 0 FAIL (T8); commit contains only intended files (T9).

## Risks & caveats
- SelectQuery.java is engine code — timing comparison is mandatory (covered by T8).
- JBR 17 is a full JDK but not a standard distribution; if `mvn test-compile` under JBR 17 hits an unexpected toolchain issue, fall back to compiling with JDK 21 using `--release 17` and report the JBR issue to the user.
- `USERS.csv` (tracked, modified) and all untracked runtime artifacts must stay out of the commit.
- CI matrix is best-effort (cannot be validated locally); verify YAML is valid and the workflow mirrors the local `mvn test` invocation.
