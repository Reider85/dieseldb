# Prompt 74: Benchmark + timing verification + commit/push

## Goal
Execute prompt 74 from `prompt.md` ("Add a simple benchmark, based on PerformanceTest, measuring insert/select operation times, output results to log or report"), run tests WITHOUT `-Ptest`, verify timings did not degrade >20% vs the previous run, and on success commit + push to `origin/main`.

## Current state (verified)
- `src/test/java/diesel/PerformanceTest.java` already contains the full prompt-74 implementation (uncommitted): benchmarks INSERT, UPDATE, TRANSACTION, READ_UNCOMMITTED, 9 SELECT queries, TRUE-condition query; logs results via SLF4J AND writes `benchmark_report.md` (avg/min/max/stddev per operation). No further code edits expected.
- Default `mvn test` (no `-Ptest`) runs only `AllTestsSampleTest` (199 checks, writes root `timingN.md` + 138-query report) and `QuantitativeTest` (199 checks). PerformanceTest is NOT in the default suite.
- Latest tracked timing report: `timing/timing55.md` (138 queries, 0 FAIL). Machine noise band on this box: ~0.5x–2.4x aggregate.
- `origin/main` == local `main` at `df11173`. Remote: `https://github.com/Reider85/dieseldb.git`.
- Root untracked timing files currently end at `timing32.md`, so the next AllTestsSampleTest run writes root `timing33.md`.
- User decision: commit includes PerformanceTest.java + benchmark_report.md + Changelog.md (2.7.58) + PHASE0_CHANGES.md note + timing/timing56.md.

## Tasks

### 1. Pre-flight
- `git status` — confirm working tree matches expectations (only `USERS.csv` + `PerformanceTest.java` modified, plus untracked artifacts).
- Confirm `mvn` runs (`mvn -version`). If missing, use the Maven wrapper/intellij maven used previously.

### 2. Full test run WITHOUT -Ptest
- Run from repo root: `mvn test` (NO `-Ptest`), redirect output to `run_prompt74.log`. Use a large timeout (e.g., 3 600 000 ms) — the suite does 600x4 inserts plus join/subquery/server tests.
- Success criteria: log contains `AllTestsSampleTest results: 199 passed, 0 failed`, QuantitativeTest passes, and `BUILD SUCCESS`.
- This run creates root `timing33.md` (next free index).

### 3. Timing check — no degradation > 20%
- Compare root `timing33.md` against `timing/timing55.md` using a throwaway script in `C:\Users\user\AppData\Local\Temp\kilo\` (PowerShell or Python; parse `Time (ms)` column per row, matched by row index since query order is stable):
  - aggregate = Σnew / Σold;
  - per-row ratio = new/old;
  - degraded = ratio > 1.2; improved = ratio < 0.8; stable = the rest;
  - verify 0 FAIL rows.
- Pass criteria (established repo methodology): aggregate ratio ≤ ~1.2 and all >20% rows are sub-11ms micro-queries (environmental noise). If the first run catches heavy machine load (aggregate > 1.2, many degraded), discard it and rerun `mvn test` (writes `timing34.md`); use the clean run.
- If a genuine regression in non-micro queries is found: STOP, do not commit, report findings.

### 4. Generate & verify benchmark report (prompt-74 deliverable)
- Run `mvn -Dtest=PerformanceTest test` (still NO `-Ptest`; `-Dtest` overrides surefire includes so only PerformanceTest runs).
- Verify: test passes, `benchmark_report.md` created at repo root with the header table plus rows for INSERT / UPDATE / TRANSACTION / READ_UNCOMMITTED / SELECT / TRUE_CONDITION, and the log shows the benchmark timings.
- If PerformanceTest assertions fail → STOP, fix, do not commit.

### 5. Docs and artifacts
- Append `Changelog.md` entry `2.7.58 prompt 74 benchmark ...` in the established long-format style: what was added, AllTestsSampleTest 199/0 + QuantitativeTest 199/0 BUILD SUCCESS, timing summary (aggregate X.XXXx vs timing55, degraded/improved/stable counts, noise-band justification).
- Append a short note to `PHASE0_CHANGES.md` section 17 about `benchmark_report.md` (insert/select timings, avg/min/max/stddev, generated by PerformanceTest).
- Copy root `timing33.md` (or the clean rerun file) to `timing/timing56.md`.

### 6. Commit + push
- Stage ONLY:
  - `src/test/java/diesel/PerformanceTest.java`
  - `benchmark_report.md`
  - `Changelog.md`
  - `PHASE0_CHANGES.md`
  - `timing/timing56.md`
- Do NOT stage: `USERS.csv` (test artifact clobbered by Prompt62Test), root `timing*.md`, `run*.log`, `*.csv`/`*.table` artifacts, `src/main/` (untracked logback.xml — pre-existing from prompt 72, out of scope), `.codebuddy/`, `.idea/`.
- Commit message following repo style, e.g.:
  `2.7.58 prompt 74 benchmark (PerformanceTest): added a simple benchmark measuring INSERT/UPDATE/TRANSACTION/READ_UNCOMMITTED/SELECT/TRUE-condition execution times, logging results via SLF4J and writing avg/min/max/stddev to benchmark_report.md; AllTestsSampleTest 199/0 + QuantitativeTest 199/0 BUILD SUCCESS, timing report timing56.md (138 queries, 0 FAIL; no degradation - aggregate X.XXXx vs the previous timing55 run (N degraded / M improved / K stable within the 20% band), ...)`
- `git push origin main`.

## Risks / notes
- First timing run may catch machine load — rerun and use the clean run if aggregate > 1.2.
- `mvn test` is slow; always use a large timeout for the run command.
- Keep the commit strictly scoped; use explicit `git add <paths>`, never `git add -A`.
