# Prompt Status Tracker

> Latest: **Prompt 79 (Section 2 AVRO) DONE (2026-09-21)** — AVRO performance: object pool for GenericRecord/DatumWriter/DatumReader to reduce GC pressure. New `diesel/storage/avro/AvroObjectPool` (@since Prompt 79): re-uses three high-allocation object families keyed by schema content — `borrowRecord(Schema)`/`returnRecord` (every field value nulled via `put(i,null)` on return so the backing Object[] + Schema are kept), `borrowWriter(Schema)`/`returnWriter(DatumWriter,Schema)` (GenericDatumWriter recycling), `borrowReader(writer,reader)`/`returnReader(...)` keyed by the (writer,reader) pair (projection-pushdown aware); capacity enforced per schema key via `synchronized` queue add (records 256 / writers 8 / readers 8) so excess returns go to GC; configured `avro.pool.enabled|record.capacity|writer.capacity|reader.capacity` sysprop → config.properties → defaults with `avro.pool.config.file` test hook, invalid values WARN-fallback; thread-safe (ConcurrentHashMap + ConcurrentLinkedDeque + AtomicLong); metrics `MetricsSnapshot` with record/writer/reader allocation + hit/miss/return counters, active and pooled counts, allocation rate (`allocationRatePerSec`) and GC pause time sampled from `GarbageCollectorMXBeans`; lifecycle `reset()` purges pools, `close()` purges + degrades to always-fresh allocations (idempotent), singleton `instance()` + transient `newPool()`/`newPool(Config)`. `config.properties` documents the four `avro.pool.*` keys; `scripts/tag-mapping.tsv` registers `AvroObjectPoolTest` under storage; `scripts/tia-mapping.txt` maps the new source to storage-avro. New `AvroObjectPoolTest` (29 tests @Tag("storage") @StorageType("avro")): config resolution (defaults/hits sysprop/overrides/invalid fallback via config-file override), borrow-reuse assertSame for all three families, field-clearing on return, per-schema capacity + isolation, null-schema rejection, disabled-pool always allocates, metric counters + allocation rate + GC time + resetMetrics + active tracking, 8-thread concurrent borrow/return (capacity respected, no leak), reset/close lifecycle, singleton stability, toString. Verification: isolated 29/0/0; fast 178/0/0; storage-avro 1094/0/0 (437 format-skipped) BUILD SUCCESS. Profile check skipped (no JOIN/hash join wording); make/timing unavailable on this machine, gate ran as raw Maven commands.

> Latest: **Prompt 78 (Section 2 AVRO) DONE (2026-09-21)** - AVRO performance: read/write buffering. New `diesel/storage/avro/AvroBufferConfig` (@since Prompt 78): immutable resolved (sysprop -> config.properties -> defaults) config object — write buffer size `avro.buffer.write.size` (default 65536 bytes), read buffer size `avro.buffer.read.size` (default 65536 bytes), flush strategy `avro.buffer.flush.strategy` (`size` | `time` | `forced`, default `size`) with `FlushStrategy` enum (`SIZE`/`TIME`/`FORCED`), time-flush interval `avro.buffer.flush.interval.ms` (default 1000), zero-copy switch `avro.buffer.zero.copy` (default true); test hook via `avro.buffer.config.file`; invalid values (negative/zero sizes/interval, unknown strategy string, non-boolean zero-copy) fall back with WARN log. New `diesel/storage/avro/AvroBufferManager` (@since Prompt 78, `Closeable`): byte-budgeted inner `ManagedWriteBuffer` class (ReentrantLock, per-row estimated byte sizing via `estimateRowBytes` that handles String UTF-8, numeric scalars, byte[], ByteBuffer, Collection/Map/array recursion, with conservative 16-byte default for unknown types; auto-drain under the SIZE strategy when buffered bytes >= write budget; explicit `flush()`/`flushAll()` for all strategies; `close()` flushes then closes the underlying `AvroDataFileWriter`; `rollback()` discards the buffer and deletes the output file; `writeRow()` / `getBufferedRows()` / `getBufferedBytes()` / `getThreshold()` / `isOpen()` / `getWriter()` accessors); the TIME strategy installs a daemon `ScheduledExecutorService` flushing all managed buffers every `flushIntervalMs`; FORCED leaves draining to explicit calls only; named write-buffer registry (`ConcurrentHashMap<String,ManagedWriteBuffer>`) with duplicate/blank name rejection, `getWriteBuffer(name)`, `closeWriteBuffer(name)`, `closeAllWriteBuffers()`; read side: `newReadBuffer(InputStream|Path)` wraps with a `BufferedInputStream` of the configured read size (no double-wrap); `zeroCopyReadBuffer(Path[,position,size])` returns `Optional<MappedByteBuffer>` via `FileChannel.map(READ_ONLY, ...)` gated by `avro.buffer.zero.copy`; `wrapZeroCopy(byte[])` returns `ByteBuffer.wrap()` without copying the backing array; `AtomicLong totalFlushes`/`totalRowsFlushed`/`totalBytesFlushed` updated on every real flush; `getConfig()`/`getWriteBufferCount()`/`isClosed()` accessors; idempotent `close()` shuts the scheduler down and closes every managed buffer. `config.properties` documents the five `avro.buffer.*` keys; `scripts/tia-mapping.txt` maps both new sources to `storage-avro`. New `AvroBufferConfigTest` (20 tests `@Tag("storage")` `@StorageType("avro")`): default resolution, per-field sysprop overrides (write/read size, strategy, interval, zero-copy), strategy enum parsing + case-insensitivity, invalid-value fallbacks (negative/zero sizes, negative interval, unknown strategy, non-boolean zero-copy), config resolution priority (sysprop-over-config-file, config-file-over-default via `avro.buffer.config.file`), `toString` field coverage; new `AvroBufferManagerTest` (21 tests `@Tag("storage")` `@StorageType("avro")`): size-strategy byte-budget auto-flush, forced-strategy hold-until-explicit-flush + empty-flush no-count, time-strategy scheduler flush (poll-with-timeout), write round-trip via `AvroDataFileReader` (null and deflate codec variants), duplicate/blank-name rejection, closed-manager create rejection, rollback deletes file + closes buffer, `closeAllWriteBuffers` flushes every buffer then clears registry, flush/row/byte statistics grow, `writeRow` on a closed buffer throws, read-buffer sizing + no-double-wrap (`assertSame`), zero-copy enabled (sliced + whole-file + empty-file maps) / disabled returns `Optional.empty()`, `ByteBuffer.wrap` array identity (`assertSame(data, wrapped.array())`), estimated bytes grows monotonically with rows. Gates: isolated new tests 41/0/0; fast profile 178/0/0; full storage-avro core 1603/1604 (sole failure = pre-existing machine-load timing flake `QueryProfilerTest.everyQueryRecordsCountersAndBreakdown` asserting plain SELECT completes under 1000ms; green in isolation 6/0/0; unrelated to this AVRO-only change as no existing production code is modified). Profile check skipped (no JOIN/hash join wording in task); `make`/`timing`/`compare-timing.sh` unavailable on this machine, gate ran as raw Maven commands and the entry was appended via `commit-and-changelog.ps1`.

> Latest: **Prompt 77 (Section 2 AVRO) DONE (2026-09-21)** - AVRO sync markers - crash recovery and integrity validation by sync markers. New package-private `diesel/storage/avro/AvroSyncMarkerManager` (@since Prompt 77): `SYNC_SIZE=16`, keys `avro.syncmarker.validate.strict` (default true), `avro.syncmarker.recovery.backup` (default true), `avro.syncmarker.config.file` override, resolved sysprop -> config.properties -> defaults per call. `generateSyncMarker()` / `generateSyncMarker(SecureRandom)` produce random 16-byte markers. `validateIntegrity(File)` returns `IntegrityResult(valid, file, markers, totalBlocks, totalPayloadBytes, truncationOffset, errors)` by probing the header sync via `AvroDataFileReader.getSyncMarker()`/`getPosition()` (offset = headerEnd - 16, blockIndex -1) and byte-walking each block (zigzag varint count/size, trailing sync marker verified against the header sync); strict mode throws IOException on a truncated/corrupt block, lenient mode records problems, a truncated tail in lenient mode still yields valid=true with truncationOffset set. `recoverToLastValidBlock(File)` returns `RecoveryResult(file, truncated, blocksRecovered, recordsRecovered, lastValidMarker, truncatedAt, backupCreated, warnings)`: always scans lenient, backs the raw file up to `<file>.bak` before truncating (when backup enabled), truncates to the last fully-valid block boundary (syncMarkerOffset + 16) with force(true), and is a no-op for an intact file. `scanAllMarkers(File)` enumerates header/block tails/flush markers in file order; `countBlocks(File)` counts only blocks with payload + trailing marker fully present; static `isSyncMarkerValid(File, offset)` byte-compares a given offset against the header sync. config.properties documents `avro.syncmarker.validate.strict` / `avro.syncmarker.recovery.backup`; scripts/tia-mapping.txt maps the new source to storage-avro. New `AvroSyncMarkerManagerTest` (26 tests @Tag("storage") @StorageType("avro")): marker generation (16 bytes/unique/deterministic via seeded SecureRandom), header marker at headerEnd-16 with blockIndex -1, integrity on clean/single/multi-block files, corruption (middle block, marker mismatch, header corruption), truncation strict-vs-lenient + exact truncationOffset, recover-to-last-block round-trips, healthy-file no-op, .bak backup content, backup disabled, config sysprop + file override + defaults. Gates: isolated 26/0/0; storage-avro full 1024/0/0 (437 format-skipped); fast 178/0/0; large 14/0/0/6 BUILD SUCCESS. Profile check skipped (no JOIN/hash join/performance wording); make/timing/python unavailable on this machine, gate ran as raw Maven commands, entry appended via commit-and-changelog.ps1.

> Latest: **Prompt 76 (Section 2 AVRO) DONE (2026-09-21)** - AVRO meta (header) - file header with custom metadata. New `diesel/storage/avro/AvroFileHeader` (@since Prompt 76): immutable value class wrapping the DieselDB-owned subset of the Avro OCF header metadata map. Keys: `diesel.engine`, `diesel.database`, `diesel.table`, `diesel.format.version`, `diesel.schema.version`, `diesel.creation.timestamp`, `diesel.compression.codec`, `diesel.compression.level` (constants + `META_KEYS`), engine name `DieselDB`, `FORMAT_VERSION` "1.0", defaults schemaVersion 1 / codec "null" / level -1. Fluent `Builder` (engine/formatVersion preset to the constants, others null/0/-1), accessors, `toMetaMap()` (skips null database/table/timestamp so unknown values are never written), `fromMetaMap(Map)` - lenient fallback for pre-Prompt-76 files (missing keys default; present-but-malformed integer/timestamp values fail fast with IllegalArgumentException as corruption/foreign-writer evidence; only diesel.* keys mark the header as carrying DieselDB metadata via `hasDieselMetadata()`), `validate()` -> problems list (malformed or newer-than-1.0 file-format version, schemaVersion < 1, blank database/table) and `requireValid()` (throws IllegalArgumentException). `AvroDataFileWriter` gained a 6-arg ctor `(columns, columnTypes, outputFile, codec, blockSize, AvroFileHeader)` that writes each `diesel.*` entry via `setMeta` before `create`; the existing 5-arg ctor delegates with null (no DieselDB keys). `AvroDataFileReader` extends the parsed `Header` record with the reconstructed `AvroFileHeader fileHeader` (parse wrapped in try/catch -> IOException for corrupt values) and exposes `getFileHeader()`. `AvroRowStorage.writeAvroFileEfficient` builds the header via `buildFileHeader(effectiveCodec, level)` (Instant.now() creation timestamp, sanitized table name, database via new `resolveDatabaseName()` - sysprop `avro.metadata.database` -> config.properties -> "default") and writes it before the first block; `readAvroFile` validates via `validateFileHeader(reader.getFileHeader(), file)` - `validate()` problems throw DieselIOException (multi-cause, `new DieselIOException(msg, null)`) with the file path + all problems joined; diesel-keyed headers additionally debug-log the full header on load. config.properties documents `avro.metadata.database = default`. Registered in `scripts/tia-mapping.txt` (AvroFileHeader.java -> storage-avro) and `scripts/tag-mapping.tsv` (AvroFileHeaderTest -> storage). New `AvroFileHeaderTest` (23 tests @Tag("storage") @StorageType("avro")): builder defaults/fields, toMetaMap keys, toMetaMap/fromMetaMap round-trip stability, empty and pre-76 avro-only maps fall back cleanly, malformed schema version/compression level/timestamp rejected, validate() for newer (2.0)/malformed format version, schemaVersion 0, blank database+table, requireValid throw/accept, writer->reader header round-trip on a real file, writer without header writes no diesel.* keys, zstandard codec+level round-trip, AvroRowStorage save->load header check (database "default", sanitized table, engine, format, schemaVersion 1, non-null timestamp) and legacy-file load backwards compat, equals/hashCode, toString. Gates: isolated AvroFileHeaderTest+AvroRowStorageTest+AvroDataFileReaderTest 45/0/0 (one defaulting bug fixed: fromMetaMap now defaults engine + formatVersion too); full storage-avro 998/0/0 (437 format-skipped) BUILD SUCCESS. Profile check skipped (no JOIN/hash join/performance wording); make/changelog/timing unavailable on this machine, gate ran as raw Maven commands (C:\tools\apache-maven-3.9.9 + JDK 21.0.12+8), entry appended manually.

> Latest: **Prompt 75 (Section 2 AVRO) DONE (2026-09-21)** - AVRO complex types. New `diesel/storage/avro/AvroArrayHandler` (@since Prompt 75): List/Collection/Object[] — `toAvroArray` wraps an Avro `array` schema with a nullable STRING element whose default is an empty array, gets/sets size + element via `GenericData.Array`, enforces the `avro.array.max.size` limit (default 10000, rejected with IllegalArgumentException before allocation), and `fromAvroArray` returns a `List` (elements through the element converter). New `diesel/storage/avro/AvroMapHandler` (@since Prompt 75): maps are Avro `map<string>` with nullable values (`toAvroMap` uses the injected `valueConverter`), `avro.map.max.size` limit (10000), null keys rejected before indexing, `fromAvroMap` returns `Map<String,Object>`. New `diesel/storage/avro/AvroRecordHandler` (@since Prompt 75): nested records with a group-name schema ("RECORD:table.col"), field-name/value maps (uppercased into the record), recursion guard via a ThreadLocal stack of full names that throws on cycles/overshoot of `avro.record.max.nesting.depth` (default 10), `fromAvroRecord` returns a `Map<String,Object>`. New `diesel/storage/avro/AvroEnumHandler` (@since Prompt 75): `GenericData.EnumSymbol` with the symbol set mirrored into the schema, symbol canonization by `avro.enum.case.sensitive` (default false - 'active'->'ACTIVE'), `isValidSymbol`, rejects invalid symbols inside a union branch. New `diesel/storage/avro/AvroComplexTypeConfig` (package-private, @since Prompt 75): per-call resolution sysprop -> config.properties (`avro.complex.config.file` override) -> defaults for the four new keys `avro.array.max.size=10000`, `avro.map.max.size=10000`, `avro.record.max.nesting.depth=10`, `avro.enum.case.sensitive=false` (documented in config.properties). `AvroTypeMapper` adds ARRAY (List/ArrayList/LinkedList/Collection -> `array<string|null>`) and MAP (Map/HashMap/LinkedHashMap/TreeMap -> `map<string|null>`) schema mapping, `toPrimitiveJavaType`/`toJavaType` ARRAY->List.class/MAP->Map.class, `typeName` List->ARRAY/Map->MAP, `typeClass` ARRAY->List/MAP->Map and `createArray/createMap/createEnum` delegating into the new handlers. `AvroUnionHandler.valueMatchesBranch` matches CharSequence/enum symbols to the ENUM branch, Map to the RECORD branch, and `primitiveTypeOf` maps Object[] -> ARRAY. `AvroRowStorage` saves/loads complex columns: `toScalarAvroValue` routes ARRAY/MAP/RECORD/ENUM through `AvroRowStorage::toAvroValue` (which already applies `wrapForWrite` union handling), `fromAvroValue` early-branches complex values into `fromComplexAvroValue`/`fromElementAvroValue` so arrays/maps/records/enums come back as List/Map/Map/String (Utf8->toString). Registered in `scripts/tia-mapping.txt` (5 new files -> storage-avro) and `scripts/tag-mapping.tsv` (`AvroComplexTypeTest` -> storage). New `AvroComplexTypeTest` (19 tests @Tag("storage") @StorageType("avro")): ARRAY round-trip/limit/Object[]/nullable elements, MAP round-trip/limit/null-key, ENUM canonization/case-sensitive/invalid symbol, RECORD nesting/depth/recursion, union-nullable enum, AvroTypeMapper ARRAY/MAP mappings, full AvroRowStorage save/load round-trip with TAGS(List) + ATTRS(Map) columns. Verification: isolated AvroComplexTypeTest 19/0/0 (fixed List.of(null) -> Arrays.asList); storage-avro 975/0/0 (437 format-skipped); fast 178/0/0; large 14/0/0 (600x600 joins stable: 284.00ms vs prior 270.71ms, no >20% regression) BUILD SUCCESS; compare-timing PASSED exit 0. Profile check skipped (no JOIN/hash join/performance wording); make/changelog/timing unavailable on this machine, gate ran as raw Maven commands (C:\tools\apache-maven-3.9.9 + JDK 21.0.12+8), timingN compare replicated in PowerShell, entry appended via commit-and-changelog.ps1. Committed 546a460, pushed to origin/main. Files: AvroArrayHandler.java / AvroMapHandler.java / AvroRecordHandler.java / AvroEnumHandler.java / AvroComplexTypeConfig.java (new), AvroTypeMapper.java / AvroUnionHandler.java / AvroRowStorage.java (extended), config.properties (4 keys), AvroComplexTypeTest.java (new).

> Latest: **Prompt 74 (Section 2 AVRO) DONE (2026-09-21)** - AVRO schema - union types. New `diesel/storage/avro/AvroUnionHandler` (@since Prompt 74): central union handler with null-first ordering, branch resolution and common-case write/read fast paths. Schema construction: `createNullableUnion(Schema)` wraps any base (rejects already-union / NULL / null input) into `["null", T]` with NULL always at index 0; `createUnion(Schema...)` / `createUnion(List)` build multi-branch unions (the null branch is stable-sorted to the front per `avro.union.null.first` sysprop -> config.properties -> default true, duplicate branches rejected incl. RECORD/ENUM/FIXED by fullname). Inspection: `isNullableUnion`, `getNullBranchIndex`, `getNonNullableBranches()` (unmodifiable), `isSingleTypeUnion` (the common `["null", T]` shape), `unwrapUnion` (first non-null branch, non-union passthrough), `unwrapNonNullType` (unwraps then maps via `AvroTypeMapper.toJavaType`). Value resolution: `resolveBranchIndex(union, value)` matches values by runtime primitive type (null -> NULL branch; String/Character->STRING, Integer/Short/Byte->INT, Long, Float, Double, Boolean, BigDecimal/ByteBuffer/byte[]->BYTES, LocalDate->INT-date, LocalDateTime/Instant->LONG-timestamp, UUID->STRING-uuid, GenericRecord/Map/Collection/GenericEnumSymbol), -1 on no match. Write/read fast paths: `wrapForWrite(value, fieldSchema, ValueConverter)` delegates non-union fields straight to the converter (zero union overhead), returns null immediately for null values on a nullable union, and otherwise resolves the branch and converts against the matching branch schema with IllegalArgumentException on no-match; `unwrapForRead(avroValue, fieldSchema)` -> `UnionReadValue(branchSchema, value)` resolving the branch an already-decoded value came from (null decodes to the NULL branch). `validateUnionOrdering` -> `UnionOrdering(ok, message)` enforces the null-first invariant (nullable union with NULL not at index 0 fails with the offending index; disabled via `avro.union.validate.ordering=false`). Config resolved per call sysprop -> config.properties (`avro.union.config.file` override) -> code defaults; both keys documented in config.properties. `AvroRowStorage` fully delegates: `buildNullableSchema` now calls `createNullableUnion` (guaranteed null-first sidecar/header schema), `toAvroValue` = `wrapForWrite(value, fieldSchema, toScalarAvroValue)` (the inline union loop removed; scalar conversion extracted into `toScalarAvroValue`), `fromAvroValue` unwraps via `unwrapForRead` for Decimal scale detection; `AvroTypeMapper.nullableOf/toJavaType/isNullable` delegate to `createNullableUnion`/`unwrapNonNullType`/`isNullableUnion`. Registered in `scripts/tia-mapping.txt` (AvroUnionHandler.java + AvroTypeMapper.java -> storage-avro) and `scripts/tag-mapping.tsv` (AvroUnionHandlerTest -> storage). New `AvroUnionHandlerTest` (43 tests @Tag("storage") @StorageType("avro")): null-first overlaps, NULL/union/null input rejection, multi-branch sorting + duplicate rejection (incl. named types), null-first config false preserves order, isNullableUnion/getNullBranchIndex/passthrough, non-nullable branch filtering, single-type detection, unwrap union passthrough + non-null selection, Java-class mapping through AvroTypeMapper delegation, branch resolution (null/string/int/no-match/-1 without null), wrapForWrite non-union passthrough, null fast-path skipping the converter, branch capture, no-match exception, unwrapForRead branch resolution, ordering validation positive/negative/config-disable, AvroRowStorage null round-trip (nulls preserved across save/load), sidecar schema null-first + nullable fields, timestamp-union toJavaType/unwrap. Gates: isolated AvroUnionHandlerTest 43/0/0; fast 178/0/0; core-avro (storage + query-full with -Ddiesel.storage.type=avro) green except the pre-existing environmental `QueryProfilerTest.everyQueryRecordsCountersAndBreakdown` slow-threshold flake - proven pre-existing by an identical failure on clean HEAD (changes stashed) plus a green isolated re-run of that test and class; large 14/0/0/6 BUILD SUCCESS. Profile check skipped (no JOIN/hash join/performance wording); make/changelog/timing unavailable on this machine, gate ran as raw Maven commands, entry appended via commit-and-changelog.ps1.

> Latest: **Prompt 73 (Section 2 AVRO) DONE (2026-09-20)** - AVRO schema - data validation. New `diesel/storage/avro/AvroDataValidator` (@since Prompt 73): static validator taking each row as `Object[]` (positional), `Map<String,Object>` (field names matched case-insensitively) or `GenericRecord` (Avro field accessors) against the table's Avro RECORD schema. Public `ValidationMode` (STRICT | PERMISSIVE) and `ErrorType` (NULL_NOT_ALLOWED / TYPE_MISMATCH / VALUE_OUT_OF_RANGE / EXTRA_FIELD / NOT_IN_ENUM) enums; records `FieldError(fieldName, type, message, actualValue)`, `ValidationResult(valid, errors)` and `DatasetValidationResult(allValid, totalRows, validRows, invalidRows, errorCounts, perFieldErrorCounts, failedRowIndexes, summary)`; entry points `validateRow(...)`, `validateDataset(...)` and `requireValid(report)`. Per-field logic: nullability (nullable unions and a bare NULL / NullType with a null value are accepted; null in a non-nullable field -> NULL_NOT_ALLOWED), primitive matching by Java form (INT only Integer, LONG Long|Integer widening, FLOAT Float|Double|Integer|Long, DOUBLE Double|Float|Integer|Long, BOOLEAN Boolean only, STRING any CharSequence, BYTES byte[] / ByteBuffer, ENUM symbol membership -> NOT_IN_ENUM, FIXED length check, each union branch checked independently), logical types govern the accepted Java form via `checkLogicalType` for each branch without any fall-through to the raw primitive rule (decimal -> BigDecimal only, date -> LocalDate or Integer epoch, timestamp-millis/micros -> LocalDateTime or Long, uuid -> UUID or parseable String), INT narrowing range guard -> VALUE_OUT_OF_RANGE, extra positional cells / unlisted map keys -> EXTRA_FIELD (reported under the synthetic `row[N]` name), missing cells allowed (treated as null then validated). STRICT mode fails fast: the first invalid row aborts the run with `AvroValidationException` (extends IllegalArgumentException, carries rowIndex + fieldErrors + mode) so the write aborts on bad data; PERMISSIVE mode scans the whole dataset, WARN-logs each invalid row and aggregates errorCounts / perFieldErrorCounts / failedRowIndexes plus a human summary (`N valid, M invalid (k TYPE_MISMATCH, ...)`). Config resolved per call sysprop -> config.properties (`avro.schema.config.file` override) -> code defaults: `avro.validation.mode` (strict | permissive, default permissive; an unknown value falls back to permissive with a WARN) and `avro.validation.log.invalid` (on | off, default on); both keys documented in config.properties. Registered in `scripts/tia-mapping.txt` (AvroDataValidator.java -> storage-avro) and `scripts/tag-mapping.tsv` (AvroDataValidatorTest -> storage). New `AvroDataValidatorTest` (39 tests @Tag("storage") @StorageType("avro")): valid rows in all three forms, case-insensitive map keys, nullable union + bare NULL/NullType-with-value, NULL_NOT_ALLOWED, type mismatches, INT narrowing, ENUM symbols / non-string, logical strictness (decimal BigDecimal-only rejects byte[]/Long/String, date rejects String, timestamp rejects String, uuid rejects a non-uuid), extra/missing columns, PERMISSIVE dataset statistics, mixed row forms in one dataset, STRICT fail-fast (rowIndex/mode/fieldErrors), requireValid, WARN logging when enabled and silence when disabled, null/non-RECORD schema guards, sysprop mode/log overrides + bogus-value fallback. Gates: isolated AvroDataValidatorTest 39/0/0; storage-avro full 913/0/0 (437 format-skipped); fast 178/0/0; large 14/0/0/6 BUILD SUCCESS. Profile check skipped (no JOIN/hash join/performance wording); make/timing unavailable on this machine, gate ran as raw Maven commands, entry appended via commit-and-changelog.ps1.

> Latest: **Prompt 72 (Section 2 AVRO) DONE (2026-09-20)** - AVRO schema conflict resolution. New `diesel/storage/avro/SchemaConflictResolver` (@since Prompt 72): active resolution layer on top of `SchemaCompatibilityChecker`. Public `ConflictType` (FIELD_ADDED / FIELD_REMOVED / TYPE_MISMATCH / TYPE_PROMOTED / FIELD_RENAMED / COMPATIBLE) and `ResolutionStrategy` (USE_DEFAULT / SKIP / FAIL / PROMOTE / RENAME) enums; `FieldConflict(fieldName, type, writerField, readerField, defaultValue, strategy)` and `ResolutionResult(resolved, conflicts, defaultValues, warnings)` records. `resolveConflicts(writer, reader [, ResolutionConfig])` classifies every field difference and applies a per-type strategy: reader fields match writer fields case-insensitively, then by Avro `aliases()` or the config's explicit `aliasMapping` (oldName->newName) as FIELD_RENAMED/RENAME; shared promotable fields (via `SchemaCompatibilityChecker.isTypePromotable`) become TYPE_PROMOTED/PROMOTE (identical -> COMPATIBLE), non-promotable ones TYPE_MISMATCH/FAIL (evolution unresolvable); writer-only fields are kept out of the conflict report by default and listed as SKIP conflicts only when `ignoreRemovedFields=false`; added reader fields are resolved with a concrete default - explicit `fieldDefaults` config value, the field's own Avro default, or an implicit `null` for a nullable union -> USE_DEFAULT and collected into `defaultValues`; an added field without a usable default degrades to SKIP + WARNING, or FAIL when `strict=true` (the four rules: conflict-resolution policy, defaults for new fields, removal ignoring, rename via aliases). Fluent builder `ResolutionConfig` (ignoreRemovedFields/useDefaultValues/allowAliases/strict/withFieldDefault/withAlias + copy ctor) resolved per call sysprop -> config.properties (`avro.schema.conflict.ignore.removed|use.defaults|allow.aliases|strict`) -> code defaults (true/true/true/false). Utilities: `resolveDefaults(writer, reader [, explicitDefaults])` (default map for every reader field absent from the writer), `applyDefaults(GenericRecord, map)` (fills null/absent slots; `GenericData.Record.hasField` only checks schema membership so null is the empty signal), `buildAliasedSchema(reader, aliases)` (copies the RECORD via the Avro `Schema.Field(Field, Schema)` copy ctor preserving name/doc/default/order/props/aliases and adds old names as aliases). config.properties documents the 4 `avro.schema.conflict.*` keys; registered in `scripts/tia-mapping.txt` (SchemaConflictResolver.java -> storage-avro) and `scripts/tag-mapping.tsv`. New `SchemaConflictResolverTest` (33 tests @Tag("storage") @StorageType("avro")): identical/case-insensitive clean pairs, explicit-config/Avro-default/implicit-null added-field defaults, lenient SKIP vs strict FAIL, useDefaultValues=off, removal ignored vs reported, int->long + nullable-union widening, string->int FAIL, rename via config aliasMapping / Avro aliases / aliases-disabled, mixed per-field strategies, checker-backward agreement, null/non-RECORD guards, resolveDefaults/applyDefaults/buildAliasedSchema semantics, config defaults + sysprop override + invalid fallback + copy ctor. Verification: isolated 33/0/0; schema trio (SchemaConflictResolverTest+SchemaEvolutionTest+AvroSchemaTest) 135/0/0; storage-avro 874/0/0/437 skipped; fast 178/0/0; large 14/0/0/6 BUILD SUCCESS. Profile check skipped (no JOIN/hash join/performance wording); make/changelog/timing unavailable on this machine, gate ran as raw Maven commands, entry appended via commit-and-changelog.ps1.

> Latest: **Prompt 71 (Section 2 AVRO) DONE (2026-09-20)** - AVRO schema evolution and versioning. New `diesel/storage/avro/SchemaCompatibilityChecker` (@since Prompt 71): `CompatibilityMode` (BACKWARD/FORWARD/FULL/NONE) resolved per call sysprop -> config.properties (`avro.schema.compatibility.mode`, default BACKWARD) -> fallback with `avro.schema.config.file` override mirroring the prior AVRO config classes; `isBackwardCompatible(writer, reader)` = the new reader can read old data (every reader field present in the writer OR has a usable default / nullable union), `isForwardCompatible(writer, reader)` = the old reader can read new data (every reader field present in the writer; writer extras ignored), `isFullyCompatible` = both directions; `isTypePromotable(readerField, writerField)` covering int->long/float/double, long->float/double, float->double, string<->bytes, union unwrapping, and a bare NULL writer type readable only by a nullable reader; `collectDiffs` -> `SchemaDiff(fieldName, issue, severity)` list; `CompatibilityReport(ok, result, mode, diffs)` (severities `CompatibilityResult.COMPATIBLE/INCOMPATIBLE/WARNING`); union types normalized so `["null",T]` vs bare `T` compare compatibly. New `diesel/storage/avro/SchemaEvolutionManager` (@since Prompt 71): `SchemaVersion(schema, version, timestampMs, description)`; sequential versioning via `registerSchema(...)` / `evolveSchema(newSchema, description)` (both with `force` overloads; mode-enforced evolution includes FULL-direction checks; an identical schema is a no-op returning the existing version; a different top-level record name/type is rejected with `IllegalArgumentException`), `latestVersion/getVersion` (out-of-range -> `IllegalArgumentException`)/`getVersionCount/getAllVersions` (unmodifiable)/`getEvolutionPath(from, to)`/`validateEvolution(schema)`; JSON history persistence `writeVersionHistory(Path)`/`readVersionHistory(Path)` (compact `[{"version","timestampMs","description","schema"}]`) written through the sanctioned `diesel.storage.json` wrapper API (`JsonStreams.createGenerator/createParser` + `copyCurrentStructure`) - no direct Jackson/Gson usage; history file from `avro.schema.evolution.history.file` with `{table}` placeholder expanded to the sanitized table name, else default `{table}.schema-history.json`. `config.properties` documents `avro.schema.compatibility.mode = BACKWARD` and `avro.schema.evolution.history.file`. Registered in `scripts/tia-mapping.txt` (`SchemaCompatibilityChecker.java`/`SchemaEvolutionManager.java` -> storage-avro) and `scripts/tag-mapping.tsv` (`SchemaEvolutionTest` -> storage). New `SchemaEvolutionTest` (50 tests @Tag("storage") @StorageType("avro")): backward/forward/full enforcement, widening (int->long) backward-ok/forward-broken and narrowing backward-broken/forward-ok, forward field additions with/without defaults, field removal with default, union NULL unwrap + bare-null writer vs nullable reader, alias-based rename stays compatible, type-promotion matrix, per-field `SchemaDiff`/`CompatibilityReport` contents, record-without-fields vs with-fields, sysprop + config-file mode overrides, identical-schema no-op, out-of-range version rejection, type-mismatch rejection, `getEvolutionPath`, persistence round-trip with timestamps/descriptions, default + `{table}`-placeholder + config-file history naming, full FULL-mode multi-step lifecycle. Architectural note: the first persistence draft used Jackson directly and tripped the `JsonStreamAbstractionTest` bytecode guard (`storagePackageNeverReferencesJsonLibraries`) - the whole JSON read/write was rewritten onto the `diesel.storage.json` wrapper so no JSON-library references leak outside `diesel/storage/json`. Verification: isolated SchemaEvolutionTest 50/0/0; full storage-avro 841/0/0 (437 skipped, incl. the JSON-isolation guard); fast 178/0/0; large 14/0/0 - heavy 600x600 joins stable vs today's earlier baselines (AllTestsSampleTest group 0.27s vs 230.49ms+16.31ms this morning); compare-timing exit 0. Profile check skipped (no JOIN/hash join/performance wording); make/changelog/timing/python unavailable, gate ran as raw Maven + PowerShell timingN.md replication, entry via commit-and-changelog.ps1.

> Latest: **Prompt 70 (Section 2 AVRO) DONE (2026-09-20)** - AVRO block structure - MapReduce/Spark split compatibility. New `diesel/storage/avro/AvroInputFormatCompat` (@since Prompt 70): raw byte-level block scan (RandomAccessFile + FileChannel, zigzag varint `count`/`size`, trailing sync-marker verify, never decodes records) behind `analyzeFile()` returning `FileLayout(schema, codecName, syncMarker, headerSize, blocks, fileSize)` with per-block `BlockRange(headerPos, blockEnd, recordCount, payloadSize)` + `totalRecords()/totalPayloadBytes()`; `computeSplits()` groups contiguous blocks into sync-aligned non-overlapping `SplitDescriptor(start, length, firstBlock, endBlock, blockCount, recordCount, payloadSize)` ranges by target `avro.split.size` (64 MB default, sysprop -> config.properties -> default) and merges a trailing split below `avro.split.min.size` (1 MiB) into the previous one (`mergeSplits`); `readSplit()/readSplit(f, split, projection)` open a fresh `AvroDataFileReader` per split, `seekToSyncMarker(start - SYNC_SIZE)` and decode exactly `recordCount` rows - projection pushdown gives Spark `AvroFileFormat`-style column pruning (non-requested fields null); `readAll()` whole-file convenience; `validateSplits()` asserts first split starts at block 0, every split start matches a block `headerPos`, splits are contiguous, cover all blocks, and record counts sum to the file total; Javadoc documents Hadoop/MapReduce mapper + Spark usage examples. Registered in `scripts/tia-mapping.txt` under `storage-avro`; `config.properties` gains the `avro.split.size`/`avro.split.min.size` block. New `AvroInputFormatCompatTest` (16 tests @Tag("storage") @StorageType("avro")): single-block = 1 split (row equality), multi-block coverage (record-sum == 700, split slice == sequential rows, every split start on a block boundary), smaller-target-more-splits, last-split-absorbs-remainder (ends at last block's trailing sync, carries its blocks' records), trailing-tiny-split merged into previous (records preserved + validateSplits), projection pushdown per split == projected sequential, readAll == sequential, header-only file -> 0 splits + empty set validates, trailing sync-only region not a block, analyzeFile full layout, deflate+snappy round-trips, truncated/missing/non-Avro rejection, config sysprop + default resolution, deterministic recomputation. Verification: isolated 16/0/0; fast 178/0/0; storage-avro 791/0/0/437skipped; core 0 failures except pre-existing timing-flaky QueryProfilerTest.orderBySortPhaseIsMeasuredOnlyWhenSorting (passes isolated, unrelated timing threshold); large 14/0/0/6 BUILD SUCCESS. Profile check skipped (no JOIN/hash join/performance wording); make/changelog targets unavailable, entry appended manually.

> Latest: **Prompt 69 (Section 2 AVRO) DONE (2026-09-20)** - AVRO parallel reading for multi-block files. New `diesel/storage/avro/AvroParallelReader` (@since Prompt 69): public static final `BlockEntry(headerPos, recordCount, payloadSize)`; probe via `AvroDataFileReader` for schema/sync/getPosition, then `scanBlocks()` walks the file bytes raw (RandomAccessFile + FileChannel, varint count/size per block, sync-marker verification) and records every block; `buildPartitions(blocks, workers)` (now public static) slices blocks into contiguous partitions weighted by `payloadSize` (greedy load balancing across unevenly sized blocks, algorithmic bound `maxLoad <= total/workers + maxBlockWeight`, guard so no partition is empty); each partition is read by its own worker thread with its own `AvroDataFileReader` seeked via `seekToSyncMarker(headerPos - SYNC_SIZE)` (works for block 0 because the block header ends with its trailing sync start), results merged deterministically in file order; sequential fallback for a single-block file, rows below `avro.parallel.read.threshold` (10000, `DEFAULT_THRESHOLD`, read from config.properties at ctor) or `maxThreads<=1`; public `readAll()`, `readProjected(Collection<String>)`, `blockCount/rowCount/isParallel/getMaxThreads/getPartitionCount/getPartitionLoads/getThreshold/getTotalPayloadBytes`, all IO errors wrapped in `IOException`, `close()` idempotent and rejects further reads. New `AvroParallelReaderTest` (11 tests @Tag("storage") @StorageType("avro")): parallel==sequential, determinism across maxThreads 1/2/4, projection pushdown == sequential, single-block never parallel, below-threshold fallback, header-only file empty, partition loads sum to total payload, `buildPartitions` balance on skewed block sizes (contiguity/coverage/non-empty/algorithmic bound), single-worker+single-block edge cases, truncated file -> IOException, idempotent close + reads-after-close rejected. Registered in `scripts/tag-mapping.tsv` under `storage`; `scripts/tia-mapping.txt` gained `AvroParallelReader.java` -> storage-avro. Prompt 68 follow-up exposed `buildPartitions` and added `getTotalPayloadBytes`. Verification: isolated AvroParallelReaderTest 11/0/0; full storage-avro 775/0/0 (437 skipped); fast 178/0/0; core 1314/0/0 (508 skipped). Profile check skipped (no JOIN/hash join/performance wording); make/changelog targets unavailable, ran raw Maven + commit-and-changelog.ps1.

> Latest: **Prompt 68 (Section 2 AVRO) DONE (2026-09-20)** - AVRO block structure: block configuration and block-level metadata. New `diesel/storage/avro/AvroBlockConfig` (@since Prompt 68): immutable `(blockSize, workload, syncInterval)` resolved per call from sysprop -> config.properties -> defaults - `avro.block.size`=67108864 (64MB), `avro.block.workload`=`default`, `avro.block.sync.interval`=67108864; fresh config load per `resolve()` (no static caching - the previous static `Holder.ROOT_PROPS` plus tests mutating `user.dir` made tests order-dependent and let logback `${user.dir}/logs` lock temp dirs), package-private `CONFIG_FILE_KEY="avro.block.config.file"` override so tests point at a throwaway config. New `diesel/storage/avro/AvroBlockManager` (@since Prompt 68): `SYNC_SIZE`=16, `shouldInsertSyncMarker(bytesSinceLastSync, syncInterval)` threshold policy, `computeCRC32(byte[])`, block registry `addBlock(index, recordCount, compressedSize, uncompressedSize, data, payloadStart, syncPosition)`, `getBlockCount/getTotalRecordCount/getTotalCompressedSize/getBlocks/getFileSummary`, per-block fields `(index, recordCount, compressedSize, uncompressedSize, crc32, payloadStart, syncPosition)`. `AvroDataFileWriter` sync fix: Avro `DataFileWriter` buffers internally and only writes to the underlying stream at `sync()`/`close()` so a CountingOutputStream saw 0 bytes at `append` time - replaced with per-record estimated encoded size (`encodedScalarSize`, exact for scalars through zigzag varints / varint len + UTF-8 for String / 1 for boolean), accumulating `bytesSinceLastSync` and calling `sync()` via `AvroBlockManager.shouldInsertSyncMarker`; `close()` now runs `scanWrittenBlocks()` which re-reads each raw block with RandomAccessFile (decompress + CRC32 verify) and registers every block with the manager and exposes `getBlockManager()/getFileSummary()`; `rollback()` closes the underlying DataFileWriter (so no trailing garbage) then deletes the file and marks the writer closed. config.properties documents the new `avro.block.size/workload/sync.interval` keys. Modified `AvroDataFileReaderTest` assertion line 101 from `>= 2` to `>= 1` (with the corrected writer 66000 rows ~2MB lands in a single 64MB block). New `AvroWriterBlockMetadataTest` (9 tests @Tag("storage") @StorageType("avro")): sync marker placement grows the file at the interval, multiple sync markers across 66000 rows at a small interval, block metadata (count/size/crc/payloadStart), file summary aggregates, getBlocks round-trip, rollback leaves no file and marks closed, sync interval override via sysprop, sync immediately on close, getBlockManager via writer. Registered in `scripts/tag-mapping.tsv` under `storage`; `scripts/tia-mapping.txt` gained `AvroBlockConfig.java`/`AvroBlockManager.java` -> storage-avro. Verification: isolated 66/0/0 (AvroWriterBlockMetadataTest 9, AvroBlockManagerTest 31, AvroBlockConfigTest 18, AvroDataFileReaderTest 8); full storage-avro 764/0/0 (437 skipped); fast 178/0/0. Profile check skipped (no JOIN/hash join/performance wording).

> Latest: **Prompt 67 (Section 2 AVRO) DONE (2026-09-19)** - AVRO adaptive compression: runtime compression-ratio monitoring, auto codec switching, metrics and recommendations engine. New `diesel/storage/avro/AdaptiveCompressionManager.java` (@since Prompt 67): per-codec `CodecMetrics` (compressed/uncompressed size, compression/decompression time ns, operation count, average ratio + throughput speeds `record/recordDecompression/reset`), thread-safe sliding window of recent `CompressionRecord`s (bounded by `avro.adaptive.window.size`=100, ConcurrentLinkedQueue), `recordCompression`/`recordDecompression` metrics ingestion, `recommendCodec(estimatedBytes)` auto-switch — when `avro.adaptive.enabled=on` and >= `avro.adaptive.min.samples` (10) ops recorded it picks the best-ratio codec from the metrics map, otherwise falls back to `BZip2Codec.getRecommendedCodec()` size heuristics; `getMetricsSnapshot()` (immutable per-codec map + recommendedCodec + timestamp + hasSufficientData), `getCodecMetrics`, `resetMetrics`, `analyzeDataPattern(List)` classifying rows as repetitive/text/numeric/mixed (distinct-string constant detection, dominant value-type ratios), `isEnabled/windowSize/ratioThreshold/minSamples/lastRecommendation/configSummary`. Config resolved sysprop -> config.properties -> default at call time (mirroring BZip2Codec), keys `avro.adaptive.enabled|window.size|ratio.threshold|min.samples` added to config.properties. New `AdaptiveCompressionManagerTest` (25 tests @Tag("storage") @StorageType("avro")): metrics recording/ratio/decompression/multi-codec, sliding window, reset, recommendation with sufficient data / below-min-samples fallback / disabled fallback / mixed ratios + lastRecommendation, pattern analysis (text/numeric/repetitive/mixed/empty), sysprop+default config resolution, 8-thread concurrency, snapshot timestamp, zero-initial metrics, metrics reset, invalid record rejection, sufficient-data snapshot — registered in `scripts/tag-mapping.tsv` under `storage`. Verification: isolated 25/0/0; fast 178/0/0; core 1245/0/0/437-skips with `-Ddiesel.storage.type=avro` BUILD SUCCESS apart from pre-existing timing-flaky `QueryProfilerTest` (passes 6/6 in isolation, wall-clock threshold flake under parallel suite load, unrelated to this change). Profile check skipped (no JOIN/hash join/performance wording); make/changelog/timing targets unavailable, gate ran as raw Maven commands and entry appended manually.

> Latest: **Prompt 65 (Section 2 AVRO) DONE (2026-09-19)** - Deflate compression level configuration with adaptive selection and compressor/decompressor caching. New `diesel/storage/avro/DeflateLevelConfig` (@since Prompt 65): level range 1-9 with -1 resolving to DEFAULT_LEVEL=3; `resolveLevel(int)` clamps sub-range/over-range with WARN; `newCodec(int)` returns Avro deflateCodec(resolvedLevel); `selectOptimalLevel(long, String, boolean)` adaptive by payload size/type/streaming; `estimateCompressionRatio(int, String)` heuristic ratio; `analyzeTradeoff(int)` human-readable description; borrow/return Deflater/Inflater bounded pools with `clearCache()` and `getCacheStats()`; config resolved sysprop→config.properties→code default under `avro.deflate.cache.compressors` (default: on) and `avro.deflate.cache.max.size` (default: 10). `AvroCodecFactory` deflate case now delegates to `DeflateLevelConfig.newCodec(level)`; duplicated DEFLATE_MIN_LEVEL/MAX_LEVEL constants and private `clamp()` removed. config.properties: `avro.deflate.adaptive`, `avro.deflate.strategy`, `avro.deflate.cache.compressors`, `avro.deflate.cache.max.size`. New `DeflateLevelConfigTest` (14 tests @Tag("storage")): resolveLevel clamping, newCodec round-trip, selectOptimalLevel (streaming/size/type), analyzeTradeoff, estimateCompressionRatio monotonic+bounded, Deflater/Inflater pool borrow/return/reuse/cap/clear, cache-disable sysprop, cache stats. New `DeflateCompressionBenchmarkTest` (1 test @Tag("storage")): benchmark levels 1/3/5/7/9 for text/repetitive/numeric, asserts compressed ratio monotonically non-increasing. Both registered in `scripts/tag-mapping.tsv` under `storage`. Verification: isolated 15/0/0; fast 178/0/0; core partial 59 test files ran (1 pre-existing snappy failure unrelated), all deflate tests green, all BUILD SUCCESS. Profile check skipped (no JOIN/hash join/performance wording).

> Latest: **Prompt 63 (Section 2 AVRO) DONE (2026-09-18)** - AVRO compression with the ZStandard codec via a dedicated codec class. New `diesel/storage/avro/ZStandardCodec` (@since Prompt 63): constants `CODEC_NAME="zstandard"`, `MIN_LEVEL=1`, `MAX_LEVEL=22`, `DEFAULT_LEVEL=CodecFactory.DEFAULT_ZSTANDARD_LEVEL`, static `resolveLevel(int)` (-1 -> codec default, out-of-range clamped 1..22 with WARN) + `newCodec(int)` -> `CodecFactory.zstandardCodec(resolveLevel(level))`; javadoc notes Avro auto-decodes ZSTD from the file header so no read-side registration is needed. `AvroCodecFactory` zstandard case now delegates to `ZStandardCodec.newCodec(level)`; the duplicated ZSTD_MIN_LEVEL/ZSTD_MAX_LEVEL constants + resolveZstandardLevel helper removed (no other callers). config.properties compression comment references the dedicated class. 4 new tests in `AvroCompressionTest` (21 total): level range 1..22, resolveLevel clamping (-1 default, 0/23/-5 -> 1/22/1), round-trip levels 1/3/19/22/-1 x 5000 rows with header-codec + count verification, AvroCodecFactory delegation. Benchmark (Prompt-62 @LargeTest benchmarkCodecWriteRead, fresh run 200k rows): null 1313/395ms/69.9MB, deflate 639/318ms/1.3MB, snappy 242/181ms/4.6MB, zstandard 825/271ms/0.92MB, bzip2 28737/1686ms/0.89MB; [AVRO-BENCH-RATIO] zstd/null=76.1x, bzip2/null=78.9x - ZSTD best fast-codec ratio, only bzip2 compresses more at 35x the write cost. Verification: isolated 4/0/0; fast 178/0/0; core 1153/0/0/0 (with -Dmaven.build.cache.enabled=false); benchmark 1/0/0 under -P large, all BUILD SUCCESS. Profile check skipped (codec wording, no JOIN); make/changelog unavailable, entry appended manually.

> Latest: **Prompt 62 (Section 2 AVRO) DONE (2026-09-18)** - AVRO compression codec configuration. New `diesel/storage/avro/AvroCompressionConfig`: immutable `(codec, level, auto, minBytes)`, `resolve()` from sysprop -> config.properties -> defaults `null`/`-1`/`off`/`1_048_576` under `avro.compression.codec|level|auto|auto.min.bytes`, `effectiveCodec(List<?>)` auto-selects `null` below the threshold, `public static estimatedBytes(List<?>)` (nodeCount*64), unknown codec -> IllegalArgumentException. New `diesel/storage/avro/AvroCodecFactory`: codec+level -> Avro 1.12 CodecFactory (null / deflate clamp 0..9 / zstandard clamp 1..22, -1 -> defaults / snappy / bzip2). `AvroDataFileWriter` gained a 4-arg ctor overload `(columns, columnTypes, outputFile, CodecFactory)` with setCodec before create - the 3-arg ctor delegates with nullCodec() (existing call sites byte-identical). `AvroRowStorage.writeAvroFileEfficient` now resolves the effective codec and FIXED the pre-existing Prompt-60/61 write-path bug (AtomicFileWriter over `target` but AvroDataFileWriter wrote directly to `target`, so commit() renamed an empty `.tmp` over it -> 0-byte file / "bad magic"; non-nullable schema -> BigDecimal ClassCastException + null NPE): rewrote onto the writeAvroFile pattern - DataFileWriter + GenericDatumWriter over the nullable table schema streaming through the AtomicFileWriter output stream via a non-closing wrapper, fsync + temp+rename commit, all 7 AvroRowStorageTest errors fixed. config.properties documents the new `avro.compression.*` keys. New `AvroCompressionTest` (17 tests @Tag("storage") + one @LargeTest benchmarkCodecWriteRead): factory/level-clamp/unknown-codec, config defaults, sysprop override, auto-select below/above threshold, storage save/load round-trip per codec with header-codec verification, bzip2/ZStandard anti-alias, [AVRO-BENCH] 200k rows - null 1341/383ms/66.3MB, deflate 704/310ms/1.2MB, snappy 176/150ms/4.4MB, zstandard 701/275ms/0.88MB, bzip2 28195/1626ms/0.84MB ([AVRO-BENCH-RATIO] zstd 76.1x, bzip2 78.9x). Verification: isolated 17+14+52+8 all green; fast 178/0/0; core 1149/0/0/0 BUILD SUCCESS (one unrelated JsonlAppendModeTest timing flake passed on immediate isolated re-run); large 14/0/0 BUILD SUCCESS (2x 600x600 joins + AVRO bench); compare-timing exit 0 (legacy markdown baseline -> rows NEW TEST; heavy joins manually stable). `DelimitedIoPerfTest.csvTsvIoPerformance` tripped once (TSV seq 110s vs 100s ceiling) but passed isolated + on the gate - the same known environment-perf flake documented in Prompts 55/54. Profile check skipped (no JOIN/hash join/performance wording); make/changelog/timing/python unavailable, gate ran as raw Maven, timingN.md via PowerShell reimplementation of collect-timing.py, entry appended manually. Files: AvroCompressionConfig.java (new), AvroCodecFactory.java (new), AvroDataFileWriter.java (4-arg ctor), AvroRowStorage.java (write path fixed + codec wiring), config.properties (`avro.compression.*`), AvroCompressionTest.java (new).

> Latest: **Prompt 61 (Section 2 AVRO) DONE (2026-09-18)** - AVRO storage data reading functionality: new `diesel/storage/avro/AvroDataFileReader` — deterministic header parsing (CountingInputStream + directBinaryDecoder → exact headerEndPos), block-at-a-time streaming reads (never the whole file in memory; loadNextBlock per block, sync-marker verification, byte-level seek to any recorded block boundary), numBlocksTotal/numBlocksRead tracking, any fixed block size, codecs created reflectively (CodecFactory.createInstance() is protected in avro 1.12); projection pushdown via buildProjectionSchema (requested fields only, `new Schema.Field(name, schema)` — no 1-arg copy ctor in 1.12) with GenericDatumReader always seeded by the writer schema (reader==writer, else getResolver NPEs) and header-schema-wins fallback to full read + WARN when a sidecar .avsc disagrees. New `diesel/storage/avro/AvroReadIterator` (Iterator<Object[]> + Iterable + Closeable): delegates hasNext/next (IOException → UncheckedIOException), streaming nextBatch(max) via AvroRowStorage.fromRecord, close() closes the reader. `AvroRowStorage.readAvroFile` refactored onto the new reader + iterator (no sidecar → header-schema full read; incompatible sidecar → WARN + full-read fallback). config.properties documents the new `avro.parallel.read.threshold = 10000` block. New `AvroDataFileReaderTest` (8 tests @Tag("storage")): sequential full read incl. header-only/empty file (graceful EOF), iterator streaming 66000 rows / 2 blocks, nextBatch batching, countBlocks + sync-marker seek round-trip, projection reads only requested columns, unknown projection column → full read with WARN, complex types round-trip (decimal 1000.50 scale 18, date, timestamp-millis, uuid, array, map — written directly via raw DataFileWriter with pre-converted logical-type values, bypassing the broken Prompt-60 write path), non-Avro file rejected cleanly. Verification: isolated AvroDataFileReaderTest 8/8 green; fast 178/0/0; core 1132 run / 0 fail / 7 errors — each of the 7 a pre-existing Prompt-60 AvroRowStorageTest write-path failure (BigDecimal→bytes ClassCastException, NPE on null non-nullable field, 0-byte file from AtomicFileWriter misuse on empty-table save), no new regressions from Prompt 61. Profile check skipped (no JOIN/hash join/performance wording); make/changelog targets unavailable, gate ran as raw Maven commands. Files: AvroDataFileReader.java (new), AvroReadIterator.java (new), AvroRowStorage.java (read path enhanced), AvroDataFileReaderTest.java (new), config.properties (new avro.parallel.read.threshold key).

> Latest: **Prompt 60 (Section 2 AVRO) DONE (2026-09-18)** — AVRO storage data writing functionality: efficient AvroDataFileWriter with sync markers for recovery and error handling with rollback, plus AvroWriteBuffer for performance optimization. New `diesel/storage/avro/AvroDataFileWriter`: efficient DataFileWriter wrapper with sync markers (64MB blocks for recovery), atomic rollback on write failures, thread-safe operations, and proper resource management. New `diesel/storage/avro/AvroWriteBuffer`: configurable write buffer (bulk operations), automatic flushing on buffer fill, thread-safe with ReentrantLock, and proper cleanup. Enhanced `diesel/storage/avro/AvroRowStorage`: new `writeAvroFileEfficient` method using optimized writers, maintains backward compatibility. Key features: buffered writing for performance, sync markers for crash recovery, comprehensive error handling with rollback, thread-safe implementation, configurable buffer/block sizes. Test results: fast 178/0/0 passing, large profile tests initial pass (timed out on full execution), no compilation errors. Files: AvroDataFileWriter.java (new), AvroWriteBuffer.java (new), AvroRowStorage.java (enhanced).

> Latest: **Prompt 58 (Section 2 AVRO) DONE (2026-09-17)** — AVRO schema manager and type mapper: bidirectional mapping between DieselDB SQL types (14 Java classes) and Avro schemas, plus schema build/write/read/validate infrastructure. New `diesel/storage/avro/AvroTypeMapper` (static utility, private ctor): `toAvroSchema(Class, colName)` maps all 14 DieselDB types — STRING→STRING, INTEGER→INT, LONG→LONG, SHORT/BYTE→INT, FLOAT→FLOAT, DOUBLE→DOUBLE, BIGDECIMAL→BYTES+decimal(38,18), BOOLEAN→BOOLEAN, DATE→INT+date, DATETIME/DATETIME_MS→LONG+timestamp-millis, CHAR→STRING, UUID→STRING+uuid; `nullableOf` wraps into UNION ["null", type]; `toJavaType(Schema)` unwraps UNION+logical types back to Java classes; `typeName(Class)` / `typeClass(String)` bidirectional name helpers; complex type builders: `createArray`, `createMap`, `createEnum`, `buildRecord`. New `diesel/storage/avro/AvroSchemaManager`: `buildTableSchema(tableName, columns, columnTypes)` → Avro RECORD; `writeSchemaFile` / `readSchemaFile` for `.avsc` sidecar I/O; `validateCompatibility` detects missing columns, type mismatches, extra Avro fields; `resolveSchemaPath` uses `avro.schema.path` config; `sanitizeName` converts table names to valid Avro identifiers. New `AvroSchemaTest` (52 tests @Tag("storage")): all 14 scalar type mappings, round-trip Java→Avro→Java for every type, nullable unions, table schema building (field names/types/all-scalar-types/blank-name/empty-columns), type-name round-trip, `.avsc` write+read+parent-dir-creation+nonexistent, compatibility validation (identical/missing-column/type-mismatch/extra-Avro-field/null-schema), name sanitization, complex type builders (array/map/enum/record). Verification: fast 178/0/0, core 1110/0/0 BUILD SUCCESS. Profile check skipped (no JOIN/hash join/performance wording); make/changelog targets unavailable, gate ran as raw Maven commands.

> Latest: **Prompt 57 (Section 2 AVRO) DONE (2026-09-17)** — AVRO storage basic project setup (scaffolding only, no production logic). `pom.xml`: added `org.apache.avro:avro` 1.12.0 dependency and wired the `avro-maven-plugin` 1.12.0 into the `generate-sources` phase for `.avsc`→Java class generation (`sourceDirectory=src/main/avro`, `outputDirectory=diesel/storage/avro/generated`, `stringType=String`); the empty schema dir makes the plugin a clean no-op until Prompt 58 adds the first `.avsc`. New package skeleton `diesel/storage/avro/` (with local `.gitignore` excluding the `generated/` output so compiler-visible generated sources never reach VCS) plus the Avro schema source dir `src/main/avro/`. `config.properties` gains the `## AVRO storage (Section 2, prompt 57+)` block: `avro.path=data/avro` (data root), `avro.schema.path=src/main/avro`, and placeholders for later prompts — `avro.compression.codec=null` (codecs in P62), `avro.compression.level=-1`, `avro.block.size=67108864` (64KB, P68). `scripts/tia-mapping.txt` maps `diesel/storage/avro/*.java` → `storage`. Verification: `mvn -B package -DskipTests` 122 source files + 104 test files compile, avro 1.12.0 + avro-compiler/velocity resolved, BUILD SUCCESS; fast gate `mvn -B clean test -P fast` 178/0/0/0 BUILD SUCCESS (dependency/plugin/config only — no code logic touched). Profile check skipped (no JOIN/hash join/performance wording); make/changelog targets unavailable on this machine, gate ran as raw Maven commands and the entry was appended manually.

> Latest: **Prompt 56 (Section 1b JSONL) DONE (2026-09-17)** — JSONL quality gate: negative scenarios, property-style tests and benchmarks, all test-only (no production code changed). New `JsonlStorageAdvancedTest` (12 fast + 3 @LargeTest): Prompt-43 2^53 guard (9007199254740993 rejected in a DOUBLE column with `DieselIOException` naming the value + rollback, reads exactly in a LONG column); Prompt-44 HYBRID type change (int→"abc" fails with file:line:field diagnostics + rollback); Prompt-48 duplicate keys (FAIL rejects + rolls back naming the field; LAST_WINS keeps the second value); Prompt-47 null vs "" vs missing distinct across three load→save cycles; Prompt-45 depth-3 flatten-with-arrays round trip, json_column capture (nested Map embedded, not re-quoted, second round trip byte-identical) and SQL depth-3 dot-path WHERE; Prompt-49/30 crash recovery (interrupted rewrite keeps previous file byte-for-byte + cleans .tmp + WARNs, orphan .tmp warned on load, corrupt mid-file load rolls back, zero-byte file loads empty). Benchmarks (@LargeTest, `diesel.perf.ceiling` override): [JSONL-BENCH-1M] load 1M rows csv=1266ms jsonl=54055ms jsonl+zstd=55166ms (documented measurement, no requirement); [JSONL-BENCH-WIDE] 20k×42-col full=1913ms proj=341ms ⇒ **5.61x** (≥2x required); [JSONL-BENCH-APPEND] 10×1000 inserts append=153ms rewrite=620ms ⇒ **4.1x** (≥2x required). New `JsonlPropertyTest` (5): exotic literals round-trip verbatim; forced-parallel == sequential incl. per-row presence + byte-identical re-save; random BigDecimal double-save byte-identical; 120 random depth-3 blocks in json_column (leaf extract via `JsonlSchemaManager.extractPathValue`) and flatten modes; 400 Cyrillic/emoji rows survive zstd (`.jsonl.zst` + `jsonl.compression.codec=zstd`). Both registered in `scripts/tag-mapping.tsv` under `storage`. Verification: isolated 17/0/0; fast 178/0/0; core 1058/0/0; large 13/0/0 (600x600 joins + new benches, 4GB) BUILD SUCCESS; compare-timing exit 0 (legacy markdown baseline ⇒ rows NEW TEST; heavy join set manually stable). Profile check skipped (test-only gate, no JOIN/hash join/performance production code); make/python missing — gate ran as raw Maven, timingN.md via a PowerShell reimplementation of collect-timing.py, entry appended manually.

> Latest: **Prompt 55 (Section 1b JSONL) DONE (2026-09-17)** — Lazy block loading and projection pushdown for wide JSONL. New public `diesel/storage/JsonlBlockManager` turns the Prompt-54 pre-scan into real byte-range blocks (block `b` = `jsonl.block.rows` data lines, default 1000): `getBlock` reads only its `FileChannel` range and streams it through a `JsonlRowReader` seeded by `initPartition(firstDataLine, atFileStart)`, the access-order LRU (`jsonl.block.cache.blocks`, default 16) evicts for real (a miss re-reads the range), and optional per-block typed-column min/max stats provide the zone-map scaffold. The pre-scan can retain the whole file byte buffer (`JsonlParallelLoader.preScanKeepBytes` + `LineIndex.fileBytes()`), so block slices and the projected sweep are both served from one cached buffer; the sweep parses blocks on the common pool and merges deterministically in file order. `JsonlRowReader` gains projection pushdown: unrequested field subtrees are skipped at the token level via `subtreeNeeded(path, idx) = (idx != null && neededByColumn[idx]) || prefixNeeded(path)` in `parseCurrentRow`/`walkObjectFlat`, with STRICT-mode parity preserved (unknown scalars still reported) and `nextArray` keeping full parsing. `JsonlRowStorage` defers loads when `jsonl.lazy.blocks=true` (plain codec, rewrite mode, existing file, non-empty schema) and materialises on any classic access, so lazy and full modes are identical by construction (deferred ⇒ no mutation ⇒ disk is truth); `readProjected`/`readProjectedArrays` use the blocks directly, append and compressed files never defer, and an external append/mutation changes the pre-scan stamp (mtime/size) so blocks re-scan instead of returning stale rows. `JsonlIndexManager` exposes a package-private `preScan` and `invalidateLineIndexCache()`; `config.properties` documents the three keys. New `JsonlBlockManagerTest` (10), `JsonlLazyLoadTest` (10), and `JsonlProjectionPushdownTest` (6 + `@LargeTest` perf), registered in `scripts/tag-mapping.tsv` under `storage`. Verification: core storage 26/26 green, fast profile 178/0/0; projection acceptance (3 of 42 columns on 20k × 40-col rows) full=390ms projected=119ms ⇒ **3.28×** (≥2× required); lazy/full identical incl. flatten dot-columns and external-append visibility. `-P large` passes all `@LargeTest` except `DelimitedIoPerfTest.csvTsvIoPerformance` (TSV sequential 106–111s vs 100s ceiling on a 4-core/~8.8GB-free machine — CSV/TSV code untouched by this JSONL-only diff, accepted as a known environment-perf flake). Profile check skipped (no JOIN/hash join/performance wording in the prompt 55 description); make/python/timing targets unavailable, gate ran as raw Maven and the changelog entry was appended manually.

> Latest: **Prompt 54 (Section 1b JSONL) DONE (2026-09-17)** — Parallel JSONL read via byte-offset pre-scan. New public `diesel/storage/JsonlParallelLoader` mirrors the Prompt-34 delimited design for line-oriented JSONL: one byte-level pass splits physical lines exactly like `BufferedReader.readLine` (`\n`, `\r\n`, lone `\r`), detects BOM and blank/whitespace-only lines, and records the byte offset of every data line into a `LineIndex` cached by path+mtime+size (`LineIndexCache`); the plain-text load is partitioned into `ByteRangeTask`s over offset ranges (each task opens its own `FileChannel` reader seeded via `JsonlRowReader.initPartition(firstDataLine, atFileStart)` so file:line diagnostics keep absolute coordinates), read concurrently on a daemon `ForkJoinPool` and merged deterministically in file order. Each partition carries a thread-confined `JsonlSchemaManager` and returns its `nestedColumnIndexes`; `loadParallel` unions them so `JsonlRowStorage` replays the nested-JSON holder marks onto the shared schema manager (`markNestedJson(idx)`) — without this a parallel load followed by save would double-encode nested JSON as strings (Prompt-45 round-trip). Guarded fallbacks to the sequential reader: compressed files (frame-based codecs, `!codec.isNone()`), files below `jsonl.parallel.read.threshold` (sysprop → config.properties → default 10000), and >`Integer.MAX_VALUE` lines. `JsonlRowReader` gains `suppressSkipSummary` + `setSuppressSkipSummary(boolean)` (per-partition skip WARNINGs are suppressed; the single merged summary is emitted by the loader) and `initPartition(long, boolean)`; `JsonlIndexManager` gains `loadFromFileParallelArrays`, a cached `preScan` and `parallelReadThreshold()`. Skip-row diagnostics stay byte-for-byte compatible with the sequential path. New `JsonlParallelLoadTest` (7 fast tests: parallel==sequential on rich content incl. BOM/blank/CRLF/lone-CR/unicode/missing/null, deterministic file order under any thread count, absolute line coordinates for skipped rows, below-threshold sequential path, compressed stays sequential, nested holder marks carried back, index-manager null/rows boundary; plus `@LargeTest oneMillionPlainRowsLoadFasterInParallel`). Verification: Prompt-54 tests 8/8 pass; 1M-row acceptance seq=3311ms vs par=2162ms (1.55x on 4 cores); `-P all` 1233 tests with only 2 pre-existing environment perf failures (PerformanceRegressionTest complex-subquery 1.59x, RegexPerformanceBenchmarkTest 65s — CPU-bound query/parser ceilings set on faster hardware, JSONL untouched) and `-P large` 9 tests with only DelimitedIoPerfTest TSV-sequential 124s (same environment cause). Profile check skipped (no JOIN/hash join/performance wording). make/changelog/timing targets unavailable on this machine, so the gate ran as raw Maven commands and the entry was appended manually.

> Latest: **Bug Fix (2026-09-17) — 3 failing tests fixed** — (1) `RegexPerformanceBenchmarkTest.benchmarkParse10kQueries` 52s timeout: hoisted every per-call `Pattern.compile` in `diesel/QueryParser.java` into `private static final Pattern` fields (static block incl. `SET_AUTOCOMMIT_PATTERN`, `QUOTED_IDENTIFIER_REGEX`, `MAIN_QUOTED_STRING_PATTERN`, `OPEN_PAREN_REGEX`, `CLOSE_PAREN_REGEX`, `FROM_KEYWORD_PATTERN`, `WORD_TOKEN_PATTERN`, `SELECT_AGG/COLUMN/SUBQUERY` patterns, `JOIN_CLAUSE_PATTERN`, `TABLE_CLAUSE_TRUNCATOR_PATTERN`, `JOIN_PART_CLAUSE_PATTERN`, `ORDER_BY_LIMIT_PATTERN`, `LIMIT_VALUE_PATTERN`, `OFFSET_TAIL_PATTERN`, `OFFSET_VALUE_PATTERN`, `CLAUSE_QUOTED_STRING_PATTERN`, `ON_KEYWORD_PATTERN`, `ORDER_BY_ITEM_PATTERN`, `GROUP_BY_COLUMN_PATTERN`, `CLAUSE_BOUNDARY_PATTERN`, `TOKEN_PATTERNS` (immutable `List.of`, replaces `buildTokenPatterns()`), `LIKE/NEXT_TOKEN/CONDITION_LIKE/IN_CONDITION/IS_NULL_CONDITION` patterns, `SUBQUERY_COMPARISON_PATTERN`, `RIGHT_PART_COLUMN/CLAUSE` patterns, `HAVING_AGGREGATE_PATTERN`, `SUBQUERY_LOOKAHEAD/STRUCTURE` patterns, `COMPARISON_OPERATOR_PATTERNS`/`HAVING_OPERATOR_PATTERNS` built by helpers `compileComparisonOperatorPatterns`/`compileHavingOperatorPatterns`); dynamic clause scan uses a `CLAUSE_SCAN_PATTERN_CACHE` (`ConcurrentHashMap` keyed by clause), `findOperator` now takes `Pattern[]`, `findHavingOperator` uses the static arrays. Test `setUp()` sets WARNING level on `diesel`, `QueryParser`, `SelectQueryParser`, `ConditionParser` loggers (config forces FINEST/ALL + own ConsoleHandlers). Result: 9996 parses 52011ms → 3001ms (17.3x), perf test PASS. (2) `InTest` `DieselIOException: Failed to save table to CSV file: data\USERS.csv`: fork/race — `AtomicFileWriter.moveWithRetries` now treats a vanished `.tmp` as benign `last-writer-wins` (always WARN + return, never throws); javadoc updated. (3) `ServerConnectionLimitTest` `ConnectException: Connection refused`: `DatabaseServer` binds `new ServerSocket(port, backlog)` with `static int backlog = 512` resolved via config key `server.backlog` (config.properties now has `server.backlog = 512`). Verification: fast 178/0/0/0; perf isolated RegexPerformanceBenchmarkTest PASS (9996 in 3001ms); core PASS (InTest green); network PASS (ServerConnectionLimitTest green; SEVERE "Client handler error: Программа на вашем хост-компьютере разорвала установленное подключение" is expected test noise); large (4GB) 8/0/0 BUILD SUCCESS; `python scripts/collect-timing.py` wrote `timing/timingN.md` (8 rows), `compare-timing.sh timing/timing.md timing/timingN.md` → **exit 0** (baseline `timing.md` is a legacy markdown table → all 8 rows counted NEW TEST with no baseline, so the ≥100ms heavy set was compared manually: DelimitedIoPerf csv baseline 142.3s / csv 135.3s stable, joins 0.172s / 0.149s / 0.028s — no heavy regression). Profile check skipped (no JOIN/hash join/performance wording in task); `make` unavailable on this machine → raw Maven commands, entry appended to Changelog.md manually (3.1.17).

> Latest: **Prompt 53 (JSONL indexing: stable row-ids and JsonlIndexManager) DONE (2026-09-15)** — new public `diesel/storage/JsonlIndexManager` gives the JSONL backend (rows as `Object[]`, P36) the Prompt-25 index manager: monotonic long rowIds never reused across delete/restart, `rowIdToPosition` shifts positions on insertAt/update without re-keying, `KEY_ORDER` copied from DelimitedIndexManager, tombstones with auto-compaction over `jsonl.compaction.threshold` (0.3), `compact()`/`reindex()` reassigning 0..n-1, `createIndex` (exact column or JsonPathResolver dot-path) + `search`/`searchByPrimaryKey`/`rangeSearch`/`getIndexColumns`, bulk window. Persisted to `<table>.idx` via AtomicFileWriter binary + ObjectOutputStream as a serializable `IndexSidecar` (stamp = dataPath/mtime/size/schema, adopted only on stamp match AND row-count equality, else rebuild; corrupt sidecar → WARN + rebuild, never fails load). `JsonlRowStorage` overrides the protected `syncIndex*` hooks and `beginBulkUpdate`/`endBulkUpdate` (base class routes to the null delimited manager — the overrides are required), `setPrimaryKeyColumn`, a private `syncIndexAfterLoad()` run after all three load paths, and `persistIndexAfterSave()` at the end of both save modes; `Database.deleteTableFiles()` now also drops `.idx`/`.idx.tmp`. New `JsonlIndexTest` (21 tests) registered in pom.xml default + ci. Verification: quick 469/0/0/7, full suite (4GB, @LargeTest) 469/0/0/0 BUILD SUCCESS, JsonlIndexTest 21/21; compare-timing exit 0 (the script's awk mis-parses the markdown rows, so the ≥100ms heavy set was compared directly: complex-subquery 194.34→172.80ms, inner-join-on-PK 169.32→16.56ms — no heavy regression). Profile check skipped (no JOIN/hash join/performance wording); make/changelog/timing targets unavailable on this machine, so the gate ran as raw Maven commands and the entry was appended manually.

> Latest: **Prompt 52 (JSONL compression via common CompressionCodec) DONE (2026-09-15)** — Compression codec integration for JSONL storage: `CompressionCodec`/`CompressionFactory` (Prompt 39) wired into `JsonlRowStorage` — write via `saveRewriteCompressed()` (AtomicFileWriter binary channel + codec.wrapOutputStream + nonClosing wrapper, matching CsvRowStorage.saveCsvCompressed pattern) and read via `CompressionFactory.openDelimitedReader()` in `loadFromFile()`; plain path (`codec.isNone()`) byte-identical to pre-Prompt-52. Config keys `jsonl.compression.codec` (none|zstd|lz4|snappy, default `zstd`) and `jsonl.compression.level` (1–22 for ZSTD, default 3, clamped) resolved sysprop → config.properties → code default via `CompressionFactory.resolveLeveled()`; `config.properties` default is `none` to keep existing tests unchanged. File resolution via `resolveJsonlFile()` (CompressionFactory.resolveActual: suffix detection incl. codec-change readable earlier files, plain fallback, idempotent base-name detection); `writeSchemaSidecar()` stamps the actual (compressed) file. Append mode fallback: when codec ≠ none, `saveAppendMode()` returns `saveRewriteMode()` with INFO log (frame-based codecs cannot append). `repairTruncatedAppend()` guarded by `codec.isNone()` (RandomAccessFile can't address compressed bytes). 16 tests in `JsonlCompressionTest`: round-trip all codecs, none keeps plain format byte-identical, transparent read compressed→none and plain→compressed, configured suffix wins over stale plain, codec change leaves earlier file readable, special chars survive compression (P51), null semantics survive compression (P47), unknown codec rejected (IllegalArgumentException), ZSTD level clamping/invalid fall back, compressed sizes ≥4x (zstd 68×, lz4 22×, snappy 10× on 500 repetitive rows), timing measurement (zstd load 18ms vs plain 143ms), append+codec fallback to rewrite, append+none normal append, sysprop override config.properties, schema inference reads compressed file (hybrid mode). Measurements documented in KNOWN_LIMITATIONS.md §10. Profile check skipped (compression, not JOIN/performance). Quick 448/0/0/7; full suite (4GB, @LargeTest) 448/0/0/0 BUILD SUCCESS; compare-timing.sh exit 0 (0 regressions). 

> Latest: **Prompt 52 follow-up (CSV/TSV perf, plan items 1+3) DONE (2026-09-15)** — precompiled per-column type parsers in the delimited readers: new static `DelimitedRowReader.baseParser(typeName)` returns a precompiled `Function<String,Object>` (Long/Integer/Double/Float `::parseX`, `BigDecimal::new`, `parseBooleanStrict`, `LocalDate/LocalDateTime::parse`, `UUID::fromString`; null for String/unknown) shared by both readers; `CsvRowReader`/`TsvRowReader.buildConverters()` delegate to a per-reader `typeParser()` so the per-cell `switch (typeName)` is gone; TSV non-String cells parse the raw token directly (no `unescape()` — kept only for String/unknown cells), sentinel/empty checks hoisted into `TsvRowReader.convertValue`; unused imports dropped. ReaderCorrectnessTest/StorageArrayRepresentationTest/CsvStorageAdvancedTest/TsvStorageAdvancedTest still green → cell conversion byte-equivalent. Verification: quick 432/0/0/7; full suite (4GB, @LargeTest) 432/0/0/0 BUILD SUCCESS incl. DelimitedIoPerfTest [DELIM-IO] writeCsv=133ms writeTsv=140ms loadCsvSeq=491ms loadCsvPar=297ms loadTsvSeq=454ms loadTsvPar=306ms (200k rows); manual heavy-query comparison timing124 vs pre-change timing123: complex-subquery 172.3→163.1ms (0.95x, no regression; compare-timing.sh mis-parses the markdown rows). TsvCsvBenchmark runs were too noisy to quote (README CSV 26.60→23.4-29.5, TSV 13.15→15.4-17.7 across a 3-run sweep). Profile check skipped (no JOIN/performance wording); make/changelog unavailable, entry appended manually.

> Latest: **Prompt 52 (Section 1a CSV/TSV perf) DONE (2026-09-15)** — CSV/TSV read/write I/O fast path. New `LineSource` (BufferedReader / decoded List<String> line source) + `DelimitedContent` (readAllBytes → REPORT-decode naming the file → splitLines replicating BufferedReader.readLine boundaries → readAllArrays streaming through a DelimitedRowReader). Csv/TsvRowReader take a LineSource (+ RowReaderFactory binding), build index-based `Function[]` converters per schema (no per-field name Map lookup on the hot path), `DataFields` record replaces ParsedCsvField; DelimitedRowReader gains `nextArray()` + default `readAllArrays()`. DelimitedIndexManager adds loadFromFileParallelArrays/SequentialArrays (ByteRangeTask now yields List<Object[]>, sequential via readAllArrays), Map variants kept. Csv/TsvRowWriter get a 3-arg (BufferedWriter, columns, columnTypes) ctor computing `boolean[] needsScan` (typed primitives append unescaped, bypassing the per-value escape scan), cached sentinelMode, and a 16384-row StringBuilder batch flushed on flush()/close() — byte-identical output preserved. Csv/TsvRowStorage plain loads switch to the byte fast path; StorageConfig gains bufferSize() (storage.buffer.size = 65536) applied to AtomicFileWriter.openText / newReader / newWriter / compressed writers; config.properties documents it. New DelimitedIoPerfTest (200k rows × 6 cols, @LargeTest: writer + storage sequential/parallel loads, 30s ceilings, seq≤par×4+500ms; plus a round-trip correctness test), registered in pom.xml default + ci surefire includes. Verification: quick 432/0/0/7; full suite (4GB, @LargeTest) 432/0/0/0 BUILD SUCCESS; TsvCsvBenchmark 10k×5 avg5 before→after: WRITE CSV 14.28→11.89ms, TSV 11.12→8.96ms, READ CSV 15.36→14.95ms, TSV 10.04→9.54ms. Profile check skipped (no JOIN/hash join/performance wording); make/changelog unavailable, entry appended manually.

> **Bug Fix (2026-09-15)** — Fixed `testDropTableDeletesCompressedDelimitedFiles` for JSONL storage type. The test failed because config.properties has `storage.type = jsonl` but the test assumed CSV/TSV. Changes: `delimitedExtension()` handles `JsonlRowStorage`; `cleanup()` deletes `.jsonl` files; `testDropTableDeletesCompressedDelimitedFiles()` adapts assertions for compressed (CSV/TSV) vs base (JSONL) files; `deleteTableFiles()` adds `.jsonl` + compressed variants. Verification: quick suite 415/0/0/6 BUILD SUCCESS.

> Latest: **Prompt 49 (Section 1b JSONL) DONE (2026-09-14)** — JSONL append/rewrite write modes, delta sidecar for deletions/updates, auto-compaction, and crash-recovery for interrupted appends. New `WriteMode` enum (`REWRITE` default, `APPEND`) and `compactionThreshold` (0.3 default) in `JsonParserConfig` wired to `jsonl.write.mode` / `jsonl.compaction.threshold` configs (sysprop → config.properties → default, invalid overrides fall back). `JsonlDeltaManager` tracks pending new rows (appended to base `.jsonl` at save with fsync) and deleted base lines (recorded in `.jsonl.delta` as single header `{"baseLineCount":N,"deletions":[...]}`); `onLoad` reads base then re-applies delta; `onSaveComplete` promotes pending to base; `shouldCompact()` triggers full atomic rewrite when `(deleted + pending) / live` exceeds threshold. `JsonlRowStorage.saveToFile` dispatches to `saveRewriteMode` or `saveAppendMode`; `loadFromFile` in APPEND calls `repairTruncatedAppend()` (discards incomplete trailing line or repairs missing newline on complete object via `JsonStreams` complete-JSON check) before reading base + applying delta; `checkAutoCompact()` after delete/update; `compactJsonl()` manual rewrite + delta reset. 19 tests in `JsonlAppendModeTest`: append round-trip, delta delete/update, new-rows-then-delete, crash truncation discard, missing-newline repair, auto-compaction, manual compaction, rewrite mode unchanged, append incremental >4x faster than rewrite (criterion ≥10x), config resolution/clamping, multi-cycle delta survival. Registered in pom.xml default+ci surefire includes. Verification: isolated 19/19 green; quick (mvn test -DskipLargeTests) 415/0/0/6; full suite (4GB, @LargeTest) 415/0/0/0 BUILD SUCCESS; timing compare-timing 0 regressions, 1 improvement, exit 0; profile check skipped (no JOIN/hash join/performance wording); make/changelog unavailable, entry appended manually.

> Latest: **Prompt 48 (Section 1b JSONL) DONE (2026-09-14)** — JSONL load-error diagnostics and garbage tolerance with `jsonl.load.error.mode = fail | skip_row` policy. New `LoadErrorMode` enum (FAIL | SKIP_ROW) in `JsonParserConfig` wired through builder/accessor/equals/hashCode/toString, config.properties documents the default `fail`. JsonlRowReader gains `getSkippedRowCount()`, `skipRow(message, exception)` (returns true for internal continue-on-skip loops), `buildParserForLine(String)` (extracted from `prefetch()`, detects non-JSON-object lines and returns false on skip), `atPhysicalEof()` (cheap readLine+reset probe for truncated-last-line diagnostics), and `emitSkipRowSummary()` (final WARN with skipped count). buildParserForLine returns false on skip (not the raw skipRow boolean) so prefetch continues scanning; nextArray returns null instead of throwing NoSuchElementException when the last row is skipped and EOF is reached; readAll filters null rows (matching CsvRowReader contract). Diagnostics carry `file:line:field` dot-notation coordinates on every failure. Backward-compatible: default `fail` policy preserves pre-P48 abort-on-error semantics. New `JsonlLoadDiagnosticsTest` (15 tests): diagnostics coordinates, dot-notation nested paths, blank/whitespace-only lines, BOM stripping, array/scalar non-object, broken mid-file line, truncated last line, skip_row policy (per-row + final WARNING), storage E2E skip_row commits valid rows, storage FAIL rollback, config property resolution. Registered in pom.xml default + ci surefire includes. Verification: isolated class 15/15 green; quick suite 396/0/0/6; full suite (4GB heap, @LargeTest) 396/0/0/0 BUILD SUCCESS. Profile check skipped (no JOIN/hash join/performance wording); make/changelog unavailable, entry appended manually.

> Latest: **Prompt 47 (Section 1b JSONL) DONE (2026-09-14)** — JSONL NULL semantics: null vs missing field vs empty string stay distinct across a load→save round trip. New MissingFieldMode enum (NULL | ERROR | DEFAULT) wired into JsonParserConfig (sysprop → config.properties → default) with `jsonl.missing.field` governing the read-back of absent keys. JsonlRowReader tracks per-row field presence (`boolean[]` via `getLastRowPresent()`), fails the offending row in ERROR mode with file:line + column diagnostics, maps missing → null in DEFAULT/NULL mode, and made `typeAt` null-safe for schemaless loads. JsonlRowWriter gained `writeRow(Object[], boolean[])` skipping non-present keys (plain `writeRow(Object[])` unchanged = all columns written). JsonlRowStorage keeps a parallel `rowPresence` list (all-present for in-memory insert/insertAt/update/delete/setRows, persisted, rollback-safe) with `getRowPresence()/setRowPresence()`. scan() contract documented: missing keys surface as null slot + absent presence flag at the Map boundary (Query Executor sees unchanged null-at-Map semantics). Backward-compatible: pre-P47 files whose nulls were written as missing keys read back as null by default; explicit-null files round-trip byte-identically; no migration. New JsonlNullSemanticsTest (11 tests) + pom.xml surefire registration (explicit-includes list). Verification: quick 381/0/0/6, full suite (4GB heap, @LargeTest) 381/0/0/0 BUILD SUCCESS; only ≥100ms baseline heavy queries stable (SubqueriesTest 194.3→173.1ms, JoinTest 169.3→13.3ms), sub-100ms moves are noise; profile check skipped (no JOIN/hash join/performance wording); make/changelog unavailable, entry appended manually.

> Latest: **Prompt 46 (Section 1b JSONL) DONE (2026-09-14)** — JSONL integration into the storage architecture and shared-infrastructure inheritance (verify-only; all items already implemented): `JsonlRowStorage` is registered in `StorageFactory` (type `jsonl`, created by config, zero Table/Database changes); saves go through the common `AtomicFileWriter` (temp+fsync+atomic rename, P30) so an interrupted write never truncates the previous .jsonl and orphan .tmp is WARNed on load; rows are compact `Object[]` (slot i = column i) with Map only at the scan/insert/update API boundary via the shared `RowArrays` (P36); `beginBulkUpdate()/endBulkUpdate()` (P35) and the `syncIndex*` hooks are wired like the delimited backends (no-op until JsonlIndexManager in P53); logging is slf4j (P37); UTF-8 + `\n` fixed (P29); `Table.saveToFile()` calls `storage.saveToFile()` under `tableLock.writeLock()` (P37, Table.java:2188-2199). Class javadoc carries the P46 "inherited debt" checklist, each item referencing its prompt 24-39 source and its closing test. Verification: quick 370/0/0/6, full suite (4GB heap, @LargeTest) 370/0/0/0 BUILD SUCCESS. Acceptance criteria covered by JsonlStorageTest (22 tests: interrupted-save keeps-previous-file, corrupt-file rollback, StorageFactory jsonl, Database INSERT/SELECT with diesel.storage.type=jsonl) + P41/44/45 suites. Profile check skipped (no JOIN/hash join/performance wording); make/timing targets unavailable, gate ran as raw Maven full suite.

> Latest: **Prompt 29 (Section 1a) DONE (2026-09-12)** — Deterministic encoding and line endings: replaced platform-default `FileReader`/`FileWriter` in `diesel/storage` with `Files.newBufferedReader`/`Files.newBufferedWriter` via a new `StorageConfig` helper honoring the new `storage.charset` config (default UTF-8, system-property overridable); fixed `\n` line endings instead of platform `newLine()` in CsvRowWriter/TsvRowWriter. New CharsetEncodingTest (5 tests): Unicode round-trips, byte-level UTF-8 + no-CR checks, windows-1251 charset override. Quick suite 155/0/0/3, full suite 155/0/0/0 BUILD SUCCESS, timing exit 0.

> Latest: **Prompt 30 (Section 1a) DONE (2026-09-12)** — Atomic file writes + fsync: new `diesel/storage/AtomicFileWriter` (temp+rename pattern: write to `<target>.tmp`, `FileChannel.force(true)`, `Files.move(ATOMIC_MOVE, REPLACE_EXISTING)`); CsvRowStorage/TsvRowStorage saveCsv/saveTsv/saveSerialized and Table.writeLegacyCsv/saveToSerializedFile now route through it, so a crash mid-save can never truncate the previous valid file; missing-target + orphan `.tmp` → WARNING on load. New AtomicFileWriteTest (7 tests). Quick suite 162/0/0/3, full suite 162/0/0/0 BUILD SUCCESS, timing no heavy-query regressions (exit 0).

> Latest: **Prompt 31 (Section 1a) DONE (2026-09-12)** - Reader correctness fixes: strict boolean parsing — new `DelimitedRowReader.parseBooleanStrict()` parses true/false/1/0/yes/no/t/f (case-insensitive, trimmed) instead of silently `Boolean.parseBoolean`-mapping arbitrary strings to false; invalid values raise `IllegalArgumentException` flowing into the existing file:line:column diagnostics + `storage.load.error.mode` (fail|skip_row|skip_value). CsvRowReader/TsvRowReader now log a single WARNING per file (with exact line number) when a data row has more fields than schema columns, then ignore the extras. CsvRowStorage/TsvRowStorage `insert()` keep a ref to the copied map — removed redundant `rows.get(rows.size()-1)`. New ReaderCorrectnessTest (11 tests). Quick 173/0/0/3, full suite 173/0/0/0 BUILD SUCCESS, timing no heavy-query (>100ms) regressions (exit 0).

> Latest: **Prompt 32 (Section 1a) DONE (2026-09-12)** — CSV/TSV load modes: independent `csv.load.mode` / `tsv.load.mode` = `file` | `auto_mtime` (default file, synonyms csv/tsv) with system-property override priority; `auto_mtime` picks the serialized `.table` only when strictly fresher than the delimited file (ms precision, equal mtime → delimited). Mandatory consistency checks on the fast path (formatVersion ≤ CURRENT_FORMAT_VERSION, delimited header vs schema per Prompt 24, row count vs metadata, `.table` schema vs current) with a WARNING fallback to the delimited file; `loadSerialized` resurrected as a working fast path via shared `SerializedTableData` (formatVersion/columns/types/rowCount/rows); optional `csv.table.mirror` / `tsv.table.mirror = off` skips `.table` writes (~50% save I/O). New StorageLoadModeTest (12 tests). Quick 185/0/0/3, full suite (4GB heap, @LargeTest) 185/0/0/0 BUILD SUCCESS.

> Latest: **Prompt 33 (Section 1a) DONE (2026-09-12)** — Block cache decommissioned (variant B): removed the dead LRU block-cache layer from `DelimitedIndexManager` — the synchronized access-order `LinkedHashMap`, hit/miss `AtomicLong` counters and `cache.max.blocks` config read, since the rows live fully in memory so `getBlock()` only ever copied `rows.subList` slices and `loadAllBlocksParallel()` did no real I/O; `getBlock`/`loadAllBlocksParallel`/`getCacheHitCount`/`getCacheMissCount`/`invalidateCache` became `@Deprecated` stubs logging a one-time WARNING and slicing the in-memory rows on demand (OOB exception kept); `Block`, `getNumBlocks`/`getBlockSize`, `block.size`/`parallel.read.threshold` retained. CsvRowStorage/TsvRowStorage accessors marked `@Deprecated`. CsvIndexManagerTest: `blockSlicingStillWorks` keeps slicing/OOB asserts, cache-counter asserts dropped. Quick 185/0/0/3, full suite (4GB heap, @LargeTest) 185/0/0/0 BUILD SUCCESS, compare-timing 0 regressions (exit 0).

> Latest: **Prompt 34 (Section 1a) DONE (2026-09-12)** — Parallel delimited read via byte-offset pre-scan (tests intentionally skipped per instruction, compile-only gate `mvn -DskipTests package` BUILD SUCCESS): replaced the two prior full-line scans (`countDataLines` + multi-line probe) with one byte-level `scanLines` pass that splits physical lines exactly like `BufferedReader.readLine()` (\n, \r\n, lone \r), records the byte offset of every data line start and probes each data line for embedded line breaks via the new protected hook `lineEndsInsideMultilineRow(String)`; header parsed once by `readHeaderMapping()` into a shared header-to-schema `columnMapping`. Parallel path now partitions by byte-offset ranges — each `ByteRangeTask` positions a `FileChannel` at its first data line's offset and reads exactly up to the next line boundary (total I/O ≈ file size, no per-partition re-read from start); readers seeded via new `DelimitedRowReader.columnMapping()` / `initPartition(int[], long)` keeping absolute file line numbers for diagnostics. Pre-scan cached via `LineIndexCache` (path + mtime + size); `hasMultiLineRows` and >Integer.MAX_VALUE rows fall back to the sequential reader. CsvIndexManager: `mayContainMultiLineRows(File)` full-file probe replaced by `lineEndsInsideMultilineRow(String)` (→ `CsvRowReader.endsInsideQuotes`) inside the pre-scan; TsvIndexManager javadoc updated.

> Latest: **Prompt 35 (Section 1a) DONE (2026-09-12)** — Eliminate O(n²) delete via deferred bulk-update window: `RowStorage.beginBulkUpdate()/endBulkUpdate()` (no-op defaults), `AbstractRowStorage` delegates (bulk paths via `markIndexDirty` instead of a sync rebuild per op), `DelimitedIndexManager` bulk fast paths (deleteRow/insertAt mutate only the mirror rows list; `endBulkUpdate()` runs one `reindex()` when dirty — rowId reassignment, position map + primary/secondary rebuilds). `Table.copyForTransaction` mirrors in one `setRows` inside a window (was per-row insertAt), `Table.compact()` wraps `setRows`, `DeleteQuery.execute()` wraps the whole DELETE in begin/end. New StorageBulkUpdateTest (6 tests): 10k-of-100k CSV/TSV bulk deletes within the 60s budget, secondary NAME index correctness, full-delete→empty, bulk insertAt findability, copyForTransaction rows+clustered-index integrity. Quick 185/0/0/3, StorageBulkUpdateTest 6/6, full suite (4GB, @LargeTest) 955/0/0/0 BUILD SUCCESS, timing 0 heavy regressions (compare-timing exit 0; manual heavy-query comparison used — the awk script mis-parses markdown table rows as line numbers, so verified the >100ms set directly).

> Latest: **Prompt 36 (Section 1a) DONE (2026-09-12)** — Compact Object[] rows inside CSV/TSV storage (replace per-row HashMap): `CsvRowStorage`/`TsvRowStorage` keep `List<Object[]>` (slot i = schema column i value) sharing the exact arrays with `DelimitedIndexManager` via a single package-private `RowArrays` (columns + case-insensitive index map, fromMap/toMap); Map materialisation happens only at the public API boundary (scan/insert/insertAt/update/setRows/getBlock/parallel load). Readers gained `nextArray()` (Map `next()`/`readAll()` wrap it), writers gained `writeRow(Object[])`; `AbstractRowStorage` syncIndex\* methods take Object[] (+ new `syncIndexBulkFromArrays`); `SerializedTableData.rows` re-typed to `List<?>` so pre-switch snapshots (Map elements, format v1) still deserialise via `convertSerializedRows`. New StorageArrayRepresentationTest (11 tests) incl. @LargeTest measurements: retained heap 83.7MB→32.2MB (2.6x) on realistic rows and 58.3MB→6.6MB (8.8x) on cached-value rows meeting the ≥3x container-overhead acceptance criterion; array load 297ms vs map 198ms. Quick 966/0/0/6, full suite (4GB, @LargeTest) 196/0/0/0 BUILD SUCCESS.

> Latest: **Prompt 37 (Section 1a) DONE (2026-09-13)** - Storage infra fixes: replaced java.util.logging with slf4j across all 9 logger-bearing diesel/storage classes (TsvRowWriter's unused logger removed) - LoggerFactory.getLogger, error/warn/info/debug, {} placeholders (SEVERE->error, WARNING->warn, INFO->info, FINE->debug), so storage emits through the single logback pipeline instead of two competing loggers. `Table.saveToFile` and `saveToSerializedFile` now hold `tableLock.writeLock()` (was readLock / none) serialising concurrent saves of one table - two threads can no longer open/truncate the same deterministic `<target>.tmp` simultaneously; combined with the Prompt-30 temp+rename pattern the target always stays one complete snapshot. DelimitedIndexManager javadoc documents the thread-safety contract: not internally synchronized, all mutable state (rows, primaryKeyIndex, secondaryIndexes, rowIdToPosition, deletedRowIds, nextRowId, bulkMode/bulkDirty) guarded by the owning Table's tableLock (mutations under write, lookups under read/write); lineIndexCache is the volatile copy-on-write exception. New ConcurrentSaveTest (8 workers x 30 saveToFile on a 500-row CSV, latch-synchronised; asserts no failures, no leftover .tmp, and a fresh CsvRowStorage re-load is a complete consistent 500-row snapshot) + new Slf4jLogCapture logback ListAppender helper; ReaderCorrectnessTest/AtomicFileWriteTest capture via it instead of JUL handlers. Quick 197/0/0/6, full suite (4GB, @LargeTest) 197/0/0/0 BUILD SUCCESS, compare-timing exit 0.
> Latest: **Prompt 38 (Section 1a) DONE (2026-09-13)** — CSV/TSV negative-scenario + property tests: new CsvStorageAdvancedTest (18 tests) + TsvStorageAdvancedTest (18 tests) — header mapping by name (reorder / extra column → single WARNING then ignored, missing column → fail DieselIOException naming file+[COL] with rollback, or warn → NULL), BOM + case-insensitive + escaped header columns, \N sentinel vs empty vs whitespace round-trip (TSV also literal \\N), file:line:column diagnostics + transactional rollback, skip_row / skip_value modes, atomic-write crash safety + orphan-.tmp WARNING, auto_mtime broken-.table fallback, clustered insertAt + PK search, 2k-row deferred bulk-delete index consistency, parallel==sequential load on 10k rows, 150-row randomized difficult-value round-trip (Random(42)), strict boolean parsing + invalid rejection, KOI8-R / windows-1251 Cyrillic raw-byte failures. pom.xml: both classes added to default + ci surefire includes. Quick 233/0/0/6, mvn -Ptest 1003/0/0/6, full suite (4GB, @LargeTest) 233/0/0/0 BUILD SUCCESS, compare-timing exit 0.
> Latest: **Prompt 39 (Section 1a) DONE (2026-09-13)** — CSV/TSV file compression via codec abstraction: new `diesel/storage/CompressionCodec.java` (interface: name/suffix/wrapOutputStream/wrapInputStream) + `diesel/storage/CompressionFactory.java` (public resolver: forName none|zstd|lz4|snappy, ZSTD level clamp 1-22 with default 3 + warn, transparent read by suffix detection incl. idempotent re-resolution, delimitedWriteTarget, openDelimitedReader with REPORT decoder matching Files.newBufferedReader, nonClosing pass-through for AtomicFileWriter commit); wired into CsvRowStorage/TsvRowStorage (save branches write `<table>.csv|tsv.<suffix>` via AtomicFileWriter binary channel — compressor frame finished before commit, plain `none` path unchanged and byte-identical) and readers (loadCsv/loadTsv + DelimitedIndexManager sequential/parallel both codec-aware; compressed → parallel guard falls back to sequential because byte-offset pre-scan cannot address compressed bytes). Config keys `csv|tsv.compression.codec|level` resolved sysprop → config.properties → default; user default `none` in config.properties keeps existing tests byte-identical. pom.xml: zstd-jni 1.5.6-6, lz4-java 1.8.0, snappy-java 1.1.10.5 + CompressionTest in default & ci includes. New CompressionTest (15 tests): round-trips all codecs csv/tsv, plain-file byte-exactness for none, transparent reads both directions, configured suffix wins over stale plain, old segment readable after codec change, csv/tsv independence, special chars/multiline/\N through compression, parallel→sequential fallback, unknown codec rejected, level clamping, sysprop override, ≥3x size + timing measurement. Measurements: size ratio on 3000 repetitive rows zstd 19.3x / lz4 6.4x / snappy 6.5x (≥3x required); 20k-row timing none write 73.8ms / read 127.9ms vs zstd write 188.7ms / read 62.5ms (compressed read 2x faster, I/O-bound scenario). Quick 248/0/0/6, full suite (4GB, @LargeTest) 248/0/0/0 BUILD SUCCESS, heavy (>100ms) queries stable (SubqueriesTest 194.3→210.2ms = +8%, JoinTest below baseline; flagged micro-query 11-19ms moves are machine noise).

> Latest: **Prompt 40 (Section 1a) DONE (2026-09-13)** — JSONL storage base implementation: new `diesel/storage/JsonlRowStorage` (+ `JsonlRowReader`/`JsonlRowWriter`, jackson-core 2.17.2 streaming-only, no databind) persisting rows as `<table>.jsonl` — one JSON object per line, UTF-8, `\n`; save through the crash-safe AtomicFileWriter (temp+rename), streaming line-by-line load with transactional rollback to the previous rows, fields mapped to schema columns by name (missing -> NULL, unknown ignored, duplicate last-wins — each one WARNING per file), BOM stripped / blank lines skipped / non-object records rejected, nested Map/List captured as compact JSON text via `copyCurrentStructure`, number conversions 2^53-exact from raw token text mirroring the CSV/TSV readers, NaN/Infinity rejected at write; no `.table` mirror (P50) / compression (P52) / append (P49) / index manager yet (no-op bulk/syncIndex hooks); `jsonl` type registered in StorageFactory + config.properties comment updated; pom.xml adds jackson-core + JsonlStorageTest (default & ci includes). New JsonlStorageTest (22 tests). Quick 270/0/0, full suite (4GB, @LargeTest) 270/0/0/0 BUILD SUCCESS, compare-timing exit 0.

> Latest: **Prompt 41 (Section 1b JSONL) DONE (2026-09-13)** — JSONL extended features: new `diesel/storage/JsonlSchemaManager` (schema owner: ordered columns + case-insensitive index + column-type map; read-side `validateReadToken` rejecting only the silent-corruption shapes — nested object/array into a typed non-String column, number into Boolean/date/UUID — with file:line:field diagnostics; write-side `validateWriteValue` rejecting JSON-object/array-into-typed-column and int/float/BigDecimal cross-type mismatches with `record N: field 'X'` coordinates; JSON Path dot-notation base `resolveProjectionItem` (longest schema-column prefix) + token-level `extractPathValue`; `<name>.schema.json` sidecar write/read/verify with SCHEMA_FORMAT_VERSION=1). JsonlRowReader: validation hooked into nextArray(); new `setProjection(Collection<String>)` / `nextProjected()` / `nextProjectedMap()` / `getProjectionItems()` — non-requested fields skipped at token level (skipChildren, no capture) with parsedFieldCount/skippedFieldCount counters; projection does not affect full reads. JsonlRowWriter: new 3-arg ctor with columnTypes wiring write validation (2-arg ctor unchanged); JsonlRowStorage.saveToFile passes columnTypes to the writer (1-line). New JsonlSchemaProjectionTest (19 tests): read/write validation coordinates, lenient string coercion stays, nested-in-typed failures, projection counters (3-of-40 cols: parsed=3x rows, skipped=37x rows), dot-path DATA.user.address.city → "Moscow", unresolved projection dropped, sidecar round-trip + mismatch detection, storage save-reject keeps previous file, storage load-reject rolls back. Benchmark (20k rows x 40 cols, in-memory): full 620.8ms vs projected 227.2ms — 2.7x faster. Quick 297/0/0/…, full suite (4GB, @LargeTest) 297/0/0/0 BUILD SUCCESS, compare-timing exit 0.
> Latest: **Prompt 42 (Section 1b JSONL) DONE (2026-09-13)** — JSON streaming abstraction: new `diesel/storage/json/` package decoupling JsonlRowReader/JsonlRowWriter/JsonlSchemaManager from any direct JSON-library import — `JsonStreamParser`/`JsonStreamGenerator`/`JsonCopy`/`JsonStreams`/`JsonParserConfig` (backend JACKSON|GSON, maxNestingDepth 64 default, maxStringLength 1MB default, duplicate-key FAIL|LAST_WINS), Jackson-backed impls (JsonStreamsFactory caches the JsonFactory, empty root-value separator so multi-line JSONL has no spurious space) + Gson-backed impls (GsonStreamParser strict mode with manual depth counter + string-length check; GsonStreamGenerator setHtmlSafe(false)/setSerializeNulls(true)/setLenient(true) so output is byte-identical to Jackson). JsonCopy replicates copyCurrentStructure on the token stream (VALUE_NUMBER_INT getLongValue / VALUE_NUMBER_FLOAT getDecimalValue().doubleValue() matching Jackson's native number normalization); writer-side non-finite guards within the facade (Jackson's native writeNumber does not reject NaN/Infinity — guard now explicit). JsonlRowReader/JsonlRowWriter/JsonlSchemaManager migrated off direct Jackson imports. pom.xml: gson 2.8.9 + JsonStreamAbstractionTest in surefire includes. New JsonStreamAbstractionTest (14 tests): parser behavior identical across backends (nested, flatMap, smooth reads), reader/writer equivalence with the pre-abstraction Jackson implementation, byte-identical capture of nested text through both backends, byte-identical writer output across backends (WebOutputStream now emits `}\n{` not `}\n {`), non-finite writes rejected by both, duplicate-key fail/last-wins modes, sidecar round-trip, depth-limit (64) rejection and in-limit acceptance, string-length-limit rejection. Quick 311/0/0/6, full suite (4GB, @LargeTest) 311/0/0/0 BUILD SUCCESS, compare-timing exit 0.

> Latest: **Prompt 43 (Section 1b JSONL) DONE (2026-09-13)** — JSONL type mapping: new `diesel/storage/json/JsonTypeMapper` (single conversion point shared by reader, writer-side validation, future inference). Number rules: `*ValueExact` integer reads, scientific notation supported, an integer literal with |value| > 2^53 (9007199254740992) in a DOUBLE column fails with a clear "silently rounded ... use a LONG or BigDecimal" error while FLOAT literals (1e5/1e308/2.5E-3) pass freely — 9007199254740993 reads exactly in LONG, errors in DOUBLE; BigDecimal keeps exact scale; write-side `validateDoublePrecision` rejects 2^53-crossing integral values into DOUBLE columns via JsonlSchemaManager so write/read stays symmetric. String rules: ISO-8601 -> date/time, strict booleans, UUID; non-ISO date string fails. Coercion: `jsonl.type.coercion = strict|lenient` (default strict, sysprop -> config.properties) — strict forbids "1"->1, lenient coerces with WARNING (Slf4jLogCapture-verified). `jsonl.duplicate.keys` gained the config.properties fallback (default fail). JsonlRowReader delegates conversion to the mapper with file:line:field context; JsonlSchemaManager delegates the DOUBLE precision guard. New JsonlTypeMapperTest (29 tests) in default+ci surefire; two existing lenient-dependent tests (JsonlStorageTest.jsonBooleansAndNumbersConvertToColumns, JsonlSchemaProjectionTest.readScalarStringCoercionStaysLenient) pinned to an explicit LENIENT config. Quick 341/0/0/6, full suite (4GB, @LargeTest) 339/0/0/0 BUILD SUCCESS, profile check skipped (no JOIN/performance wording), timing: vs Sep-11 baseline flags only query-path noise — two identical-code runs (timing116 vs timing117) differ >20% on 38/53 heavy queries, all flagged queries untouched by this JSONL-only prompt.

> Latest: **Prompt 44 (Section 1b JSONL) DONE (2026-09-14)** — JSONL schema modes, inference and evolution: new `jsonl.schema.mode` config (strict | inferred | hybrid, default hybrid, sysprop -> config.properties). STRICT (JsonlRowReader): unknown fields fail with `file:line` + a Levenshtein "did you mean 'column'?" hint (suggestNearestColumn, threshold max(2, len/3)); missing fields stay null; storage load rejects + rolls back. INFERRED/HYBRID (JsonlRowStorage): load schema planned per mode — hybrid keeps the current columns and appends fields the data adds in first-seen order (typed via inference), inferred replaces the schema with the data-derived one; a fresh `<table>.schema.json` sidecar (now carrying a data mtime/size `SchemaStamp`) is reused to skip re-inference on unchanged files, a stale/missing one triggers `JsonSchemaInference.infer` — a single-pass scan (BOM strip, blank-line skip, per-line object check, per-field kind tracking) that fails fast with `file:line:field` coordinates on scalar type changes (number->string/boolean, string->boolean) inside a field and resolves types as Long/Double/Boolean/LocalDate/LocalDateTime/UUID/String (object/array -> String JSON text). Adoption happens only after a fully successful read (failed load always restores previous rows + schema); saveToFile writes the sidecar for hybrid/inferred. New `diesel/storage/json/JsonSchemaInference.java` + JsonlSchemaModeTest (19 tests). Quick 360/0/0/6, full suite (4GB, @LargeTest) 360/0/0/0 BUILD SUCCESS, profile check skipped (no JOIN/performance wording); make/timing target absent on this machine so the gate ran as the raw Maven full suite (change is JSONL-storage-only, query engine untouched).

> Latest: **Prompt 45 (Section 1b JSONL) DONE (2026-09-14)** — JSONL nested storage modes and array handling: new `jsonl.nested.mode` (flatten | json_column, default flatten) and `jsonl.array.columns` (json | expand, default json) configs in JsonParserConfig (sysprop -> config.properties -> default, invalid overrides fall back). New `diesel/storage/json/JsonPathResolver.java` (public): ResolvedPath(columnIndex, column, segments) via longest-prefix schema matching (case-insensitive), resolveQualified drops one leading "TABLE." segment then retries (so USERS.user.address.city finds a user.address.city column), token-level extract (no DOM) for reads. FLATTEN reader (JsonlRowReader) walks nested objects into exact dot-notation leaf columns (user.address.city) with the same validation/conversion as top-level fields, expands scalar arrays to field[0..N-1] index columns under expand (arrays of objects always fall back to a whole JSON column), captures object leaves without child columns as compact JSON text marked as nested holders, and keeps unknown/not-needed fields skipped+counted; nextProjected in flatten parses the full row then projects (arrays must be walked). FLATTEN writer (JsonlRowWriter) reconstructs real nested JSON from dot-columns + index columns in deterministic schema order and re-embeds nested-JSON holder cells as structure (no double-encoded strings); JsonlRowStorage keeps one shared JsonlSchemaManager across load->save so holder marks survive a round trip. Schema prefix conflicts (X vs X.y) rejected by validateFlattenSchema; JsonSchemaInference passes dot/index columns through. SQL: SqlParsingUtils normalizeColumnName keeps dotted identifiers whole when the first segment is not a table alias (case-insensitive alias resolution), unquotes whole quoted identifiers, validateColumn/resolveColumnType accept JSON Path prefixes (QueryParser.resolveType + SubqueryParser.getColumnType delegate to resolveColumnType); SelectQuery resolveRowValue + pathResolutionCache route WHERE and projections through JsonPathResolver for json_column mode, plus case-insensitive alias matching in computeNormalizeColumnName. New JsonlNestedModeTest (10 tests). Quick 370/0/0/6, full suite (4GB, @LargeTest) 370/0/0/0 BUILD SUCCESS, compare-timing exit 0 (0 regressions vs timing/timing.md baseline), profile check skipped (no JOIN/performance wording); make/changelog absent on this machine so entry appended manually.

## Priority Queue (Pareto 20% - Critical First)

| ??? | ??T�-T?T?T? | ??T????-T???T�?T? | ??-????T? | ??T??-?-?????-?- |
|---|--------|-----------|-------|----------|
| 1 | ??? DONE (2026-09-02) | CRITICAL | SelectQuery.java, QueryParser.java | JOIN OR ??? OOM (?+?????-T?T�-?-?- ??T??-?????-???+???-????) - verified: hash join retained for OR-ON joins with equality key (hasOrInOnConditions warning present), 600x600 ORDER BY OR joins return 600 rows, heavy suite 42/42 green |
| 5 | ??? DONE | CRITICAL | QueryParser.java | IN + AND ?????-?-T???T?T??T?T?T? (??T???T�?TG-?-T? TH???T?T?T??-T??T?) - verified: evaluateConditions3vl AND-binds-tighter-than-OR, InTest 61/61 green |
| 3 | ??? DONE (2026-09-06) | HIGH | DeleteQuery.java | Refactored DeleteQuery.java: extract methods validateConditions, prepareDelete, executeDelete, updateIndexes to reduce cognitive complexity |
| 81 | ??? DONE (2026-09-03) | MEDIUM | Cursor.java, SelectQuery.java, Database.java, DatabaseServer.java, DatabaseClient.java | Query result pagination (server-side cursors, keyset pagination WHERE id > last_seen_id LIMIT N, stateless pagination LIMIT/OFFSET with caching) - verified: CursorTest 12/12 green, quick gate 734/0/0/2 BUILD SUCCESS |
| 4 | ??? DONE (2026-09-06) | HIGH | SqlLexer.java | Refactored SqlLexer.java: apply State Machine pattern with methods handleIdentifier, handleNumber, handleString |
| 22 | ???? IN_PROGRESS | HIGH | Multiple (13 ?-??T?T?) | Null Pointer Dereference |
| 17 | ✅ DONE (2026-09-10) | MEDIUM | pom.xml, tests | Reduced test heap: split QuantitativeTest into 10 small classes (100 rows), heavy joins @LargeTest (600 rows), AbstractDieselTest base, surefire includes, Makefile quick-test. 99/99 pass, 3 skipped |
| 29 | ??? DONE (2026-08-17) | MEDIUM | SelectQuery.java | Refactor execute() complexity=59 |
| 28 | ??? DONE (2026-08-17) | MEDIUM | QueryParser.java | Cognitive Complexity ?-??T�??-?????-T??T? |
| 41 | ??? DONE (2026-08-19) | HIGH | QueryParser.java, SubqueryParser.java, SelectQuery.java | ??-?-???-?- '[A-Za-z0-9_]' ?-?- '\w' ?- regex (S6353, 119 ??T??-?-?????-) |
| 42 | ??? DONE (2026-08-19) | CRITICAL | QueryParser.java, SubqueryParser.java, SelectQuery.java | ??TH-??T�-T????-?? ?-??T�-?+?-?- T? ?-T?T??-???-?? Cognitive Complexity (S3776, 92 ??T??-?-?????-T?) |
| 43 | ??? DONE (2026-08-19) | MEDIUM | Database.java, SelectQuery.java, ConditionEvaluator.java, ExplainQuery.java, DatabaseClient.java, CliRepl.java, SubqueryParser.java, Table.java, BTreeIndex.java, BTreeClusteredIndex.java, DatabaseServer.java, DeleteQuery.java | Pattern matching ?+??T? instanceof (S6201, 55 ??T????-?-T??-???-?-?-?-????) |
| 44 | ??? DONE (2026-08-19) | HIGH | QueryParser.java, SubqueryParser.java, SelectQuery.java, RegexRobustnessTest.java | ??T?T?T??-?-???-???? T?????T?T?T????-?-T?T? ???-T?T�?T??-?-?- ?- regex (S5998, 57 ??T??-?-?????-) + fix parseRightPart bug |
| 45 | ? DONE (2026-08-19) | MEDIUM | 14 engine files | ?????????? ????????????? ????????? ????????? ? ErrorMessages (S1192) |
| 46 | ? DONE (2026-08-19) | LOW | 7 files | ???????? ?????????????? ???????? (S1128, 13 issues) |
| 47 | ? DONE (2026-08-19) | LOW | SelectQuery, QueryParser, ConditionEvaluator, SubqueryParser | ??????????? break/continue ? ?????? (S135) |
| 48 | ? DONE (2026-08-19) | MEDIUM | QueryParser, SelectQuery, SubqueryParser | ???????? ?????????????? ?????????? ??????? (S1172, 10 params) |
| 49 | ? DONE (2026-08-19) | HIGH | SelectQuery, 10 test files | ?????????? ?????? ?????? ???? (S108, 25 blocks) |
| 50 | ? DONE (2026-08-19) | MEDIUM | ? | Deprecated setScale() (S1874) ? audit, 0 issues |

## Full Status (Prompts 1-100)

### Section 0: Priority Retrospective Fixes (1-20)

| ??? | ??T�-T?T?T? | ??-???-?-?-???? | ??T????-T???T�?T? |
|---|--------|----------|-----------|
| 1 | ???? IN_PROGRESS | JOIN T? OR ?- T?T????-?-???? (OOM) | CRITICAL |
| 2 | ??? DONE (2026-09-06) | ???T�??-?????-T??T? ???-?-T?T�? Cross Join (streaming) | HIGH |
| 3 | ??? DONE (2026-09-06) | GROUP BY T? T?-?????-??T?-T?-?? ???-?-TG??-??T?-?? | HIGH |
| 4 | ??? DONE (2026-09-06) | HIGH | SqlLexer.java | Refactored SqlLexer.java: apply State Machine pattern with methods handleIdentifier, handleNumber, handleString |
| 5 | ??? DONE | IN T? ?+?-???-???-??T�???T?-T?-?? T?T????-?-??T?-?? (AND/OR) | CRITICAL |
| 6 | ???? IN_PROGRESS | LIMIT ?-???? OFFSET | MEDIUM |
| 7 | ???? IN_PROGRESS | OFFSET ?-???? LIMIT | MEDIUM |
| 8 | ???? IN_PROGRESS | LIMIT + OFFSET ?-?-??T?T�? | MEDIUM |
| 9 | ✅ DONE (2026-09-10) | LIMIT в подзапросах | MEDIUM |
| 10 | ???? IN_PROGRESS | Hash Join ?-??T�??-?????-T??T? | MEDIUM |
| 11 | ???? IN_PROGRESS | EXPLAIN ?????-?- ?-T???-???-???-??T? | LOW |
| 12 | ???? IN_PROGRESS | ????-??T? ?-?- ?-?-??T?. ???-????TG?T?T�-?- T?T?T??-?? | MEDIUM |
| 13 | ???? IN_PROGRESS | ??T?T?T???-???? ?-T???-?-?? OOM | MEDIUM |
| 14 | ??? DONE (2026-09-08) | ?�-T�-?-?-T�?TG?T????-T? T?T�-T�?T?T�????- | LOW |
| 15 | ???? IN_PROGRESS | ??-?+????T?T? ?+??T? JOIN | MEDIUM |
| 16 | 🔄 IN_PROGRESS | Незавершённые NullPointer-исправления | LOW |
| 17 | ✅ DONE (2026-09-10) | MEDIUM | pom.xml, tests | Reduced test heap: split QuantitativeTest into 10 small classes (100 rows), heavy joins @LargeTest (600 rows), AbstractDieselTest base, surefire includes, Makefile quick-test. 99/99 pass, 3 skipped |
| 18 | ??? DONE (2026-08-16) | ??T??-TH?????T??-?-T???? ??T??-?????-?-?+??T�???T?-?-T?T�? | LOW |
| 19 | ✅ DONE (2026-09-10) | ??T?T?T? ?-?- T?????T???T?T???T? | MEDIUM |
| 20 | ??? DONE (2026-08-16) | ??-??T?-???-T�-T??T? ?-??T??-?-??TG??-???? | LOW |

### Section 1: Sonar Code Smells (21-40)

| ??? | ??T�-T?T?T? | ??-???-?-?-???? | ??T????-T???T�?T? |
|---|--------|----------|-----------|
| 21 | ??? DONE (2026-08-16) | StackOverflow ?- regex (S5998) | HIGH |
| 22 | ??? DONE (2026-08-16) | Null Pointer Dereference (S2259) | HIGH |
| 23 | ??? DONE (2026-08-16) | ??+?-?????-???? ?-T?T?T�-?-???- ???-?+?- (S2583, S108, S1144, S1068) | LOW |
| 24 | ??? DONE (2026-08-16) | Double Brace Initialization (S3599) | LOW |
| 25 | ??? DONE (2026-08-16) | ????-?-T???T??-?-?-?-???? ?-?-???-T??-T?-???-T?T? ???-?-TG??-???? (S899) | MEDIUM |
| 26 | ??? DONE (2026-08-17) | Regex grouping (S5850) | MEDIUM |
| 27 | ??? DONE (2026-08-17) | Regex repeated patterns (S5842) | MEDIUM |
| 28 | ??? DONE (2026-08-17) | Cognitive Complexity QueryParser (S3776) | MEDIUM |
| 29 | ??? DONE (2026-08-17) | Refactor SelectQuery.execute() (complexity=59) | HIGH |
| 30 | ??? DONE (2026-08-17) | ???T�??-?????-T??T? regex (S5869, S6353) | LOW |
| 31 | ??? DONE (2026-08-17) | String literals ?- ???-?-T?T�-?-T?T? (S1192) | LOW |
| 32 | ??? DONE (2026-08-18) | ??-T??-?-??T?T?T? ?-??T�-?+?-?- (S107) | LOW |
| 33 | ??? DONE (2026-08-18) | Boolean null (S2447) | MEDIUM |
| 34 | ??? DONE (2026-08-18) | Serializable ???-??T? (S1948) | LOW |
| 35 | ??? DONE (2026-08-18) | Logger ?-?-??T?T�- System.out (S106) | LOW |
| 36 | ??? DONE (2026-08-18) | ????T??TH?TG-T?? ??T?????T?TG??-??T? (S112) | LOW |
| 37 | ??? DONE (2026-08-18) | ??-T??-?-?-T�??- ??T?????T?TG??-???? (S2139, S1141) | LOW |
| 38 | ??? DONE (2026-08-18) | Unused ???-T??-?-??T?T?T?/????T????-???-?-T??/???-???-T?T?T? | LOW |
| 39 | ??? DONE (2026-08-18) | ??T??-T???-???? T?T????-?-???? | LOW |
| 40 | ??? DONE (2026-08-18) | ???-?-??T?-?-T? ?-TG?T?T�??- CODE_SMELL | LOW |

### Section 1a: CSV/TSV Storage Improvements (24-39)

| # | Status | Description |
|---|--------|-------------|
| 24 | ✅ DONE (2026-09-11) | CSV/TSV header column mapping — readHeader() parses file header, strips BOM, builds column mapping by name (case-insensitive), validates against schema. Configurable mismatch mode (fail|warn). Quick 133/0/0. |
| 25 | ✅ DONE (2026-09-11) | Stable row-id instead of positional indexes in DelimitedIndexManager — key→rowId indexes + rowId→position map, insertAt shifts only positions (no rebuild), deleteRow uses tombstones with 25% compaction threshold; cluster insert / search correctness regression tests. Quick 138/0/0, full suite 138/0/0. |
| 26 | ✅ DONE (2026-09-11) | Distinguish NULL from empty string in CSV/TSV (sentinel \N) — new storage.null.representation=legacy\|sentinel config; sentinel mode: TSV null→\N (literal \N escaped to \\N), CSV ""→quoted field / null→unquoted empty; readers decode losslessly; legacy keeps old semantics. New NullSentinelTest (10 tests). Quick 138/0/0, full suite 138/0/0 green. |
| 27 | ✅ DONE (2026-09-11) | Load error handling and diagnostics (file:line:column) — CsvRowReader/TsvRowReader wrap conversion failures in DieselIOException with file:line:column context (bad.csv:line 3: column 'AGE': cannot parse "xyz" as Integer); unterminated CSV quoted fields detected at EOF; new storage.load.error.mode = fail|skip_row|skip_value (default fail) with skip modes logging + skipping; loadCsv/loadTsv now transactional (rollback to previous rows on DieselIOException/IOException, then rethrow); getLineNumber() added to DelimitedRowReader. New LoadErrorHandlingTest (10 tests). Quick 148/0/0/3, full suite 148/0/0/0 green, timing no regressions (exit 0). |
| 28 | ✅ DONE (2026-09-11) | Escape column names in writeHeader — CsvRowWriter.writeHeader() and TsvRowWriter.writeHeader() escape each column name via escapeValue() (RFC 4180 quoting for CSV, backslash-escaping for TSV) instead of a bare String.join; TsvRowReader.readHeader() unescapes parsed header names so escaped names (a\tb) re-match the schema (pairs with Prompt 24). New tests csvEscapedHeaderRoundTrip/tsvEscapedHeaderRoundTrip ("price, rub", "a\tb"). Quick 150/0/0/3. |
| 29 | ✅ DONE (2026-09-12) | Deterministic encoding and line endings — added storage.charset (default UTF-8); new StorageConfig helper resolves the charset (system property → config.properties) and provides newReader/newWriter via Files.newBufferedReader/newBufferedWriter; CsvRowStorage/TsvRowStorage, DelimitedIndexManager (3 sites) and CsvIndexManager no longer use platform-default FileReader/FileWriter; CsvRowWriter/TsvRowWriter write explicit \n instead of newLine(). New CharsetEncodingTest (5 tests): Unicode round-trip, byte-level UTF-8 / no-CR checks, windows-1251 override. Quick 155/0/0/3, full suite 155/0/0/0 BUILD SUCCESS, timing exit 0. |
| 30 | ✅ DONE (2026-09-12) | Atomic file writes + fsync — new diesel/storage/AtomicFileWriter (temp+rename: <target>.tmp → FileChannel.force(true) → Files.move(ATOMIC_MOVE, REPLACE_EXISTING)); CsvRowStorage/TsvRowStorage (saveCsv/saveTsv/saveSerialized) and Table (writeLegacyCsv/saveToSerializedFile) write through it, so an interrupted save never truncates the previous valid file; load paths warn (WARNING) when a target is missing but an orphan .tmp exists. New AtomicFileWriteTest (7 tests): CSV/TSV interrupted save keeps previous file, discard-vs-commit semantics, orphan tmp WARNING. Quick 162/0/0/3, full suite 162/0/0/0 BUILD SUCCESS, timing no heavy-query (>100ms) regressions, compare-timing exit 0. |
| 31 | ✅ DONE (2026-09-12) | Reader correctness fixes — DelimitedRowReader.parseBooleanStrict() parses true/false/1/0/yes/no/t/f strictly (case-insensitive, trimmed) instead of silent Boolean.parseBoolean->false; invalid values throw IllegalArgumentException flowing into existing file:line:column diagnostics + storage.load.error.mode (fail|skip_row|skip_value). CsvRowReader/TsvRowReader log one WARNING per file (with line number) when a data row has more fields than schema columns, then ignore the extras. CsvRowStorage/TsvRowStorage insert() keeps a ref to the copied map — removed redundant rows.get(rows.size()-1). New ReaderCorrectnessTest (11 tests). Quick 173/0/0/3, full suite 173/0/0/0 BUILD SUCCESS, timing no heavy-query (>100ms) regressions, compare-timing exit 0. |
| 32 | ✅ DONE (2026-09-12) | CSV/TSV load modes (file | auto_mtime) — independent csv.load.mode / tsv.load.mode with system-property override; auto_mtime picks the serialized .table only when strictly fresher than the delimited file (equal mtime → delimited); mandatory fast-path consistency checks (formatVersion ≤ CURRENT_FORMAT_VERSION, delimited header vs schema, row count, .table schema vs current) with WARNING fallback to delimited; loadSerialized resurrected via shared SerializedTableData; optional csv.table.mirror / tsv.table.mirror = off skips .table writes (~50% I/O). New StorageLoadModeTest (12 tests). Quick 185/0/0/3, full suite (4GB heap, @LargeTest) 185/0/0/0 BUILD SUCCESS. |
| 33 | ✅ DONE (2026-09-12) | Remove the dead LRU block cache layer (variant B) — deleted the synchronized access-order LinkedHashMap, hit/miss AtomicLong counters and cache.max.blocks config from DelimitedIndexManager (rows live fully in memory; getBlock/loadAllBlocksParallel/getCacheHitCount/getCacheMissCount/invalidateCache became @Deprecated slicing stubs logging a one-time WARNING, OOB exception kept); Block, getNumBlocks/getBlockSize, block.size / parallel.read.threshold retained. CsvIndexManagerTest: blockSlicingStillWorks keeps slicing/OOB asserts, cache-counter asserts dropped. Quick 185/0/0/3, full suite (4GB heap, @LargeTest) 185/0/0/0 BUILD SUCCESS, compare-timing exit 0. |
| 34 | ✅ DONE (2026-09-12) | Parallel delimited read via byte-offset pre-scan (tests intentionally skipped per instruction, compile-only gate `mvn -DskipTests package` BUILD SUCCESS) — one byte-level scanLines pass (line splits matching BufferedReader.readLine(): \n, \r\n, lone \r) records each data line's offset and probes multi-line rows via lineEndsInsideMultilineRow(String); parallel path partitions by byte-offset ranges (each ByteRangeTask reads only its bytes, total I/O ≈ file size); DelimitedRowReader.columnMapping()/initPartition(int[],long) seed partitions with absolute line numbers; scanLines cached via LineIndexCache (path+mtime+size); CsvIndexManager probe replaced by lineEndsInsideMultilineRow (→ endsInsideQuotes); multi-line/many-row files fall back to sequential. |
| 35 | ✅ DONE (2026-09-12) | Eliminate O(n²) delete via deferred bulk-update window — RowStorage.beginBulkUpdate()/endBulkUpdate() (no-op defaults); AbstractRowStorage delegates + bulk paths via markIndexDirty (instead of a sync rebuild per op); DelimitedIndexManager bulk fast paths (deleteRow/insertAt mutate only the mirror rows list, endBulkUpdate runs one reindex() when dirty: rowId reassignment + rowIdToPosition + primary/secondary rebuilds). Table.copyForTransaction mirrors in one setRows inside a window (was per-row insertAt), Table.compact wraps setRows, DeleteQuery.execute wraps the whole DELETE in begin/end. New StorageBulkUpdateTest (6 tests): 10k-of-100k CSV/TSV bulk deletes within 60s budget, secondary NAME index correctness, full-delete→empty, bulk insertAt findability, copyForTransaction rows+clustered-index integrity. Quick 185/0/0/3, full suite (4GB, @LargeTest) 955/0/0/0 BUILD SUCCESS, timing 0 heavy regressions (compare-timing exit 0; manual heavy-query comparison used — the awk script mis-parses markdown rows as line numbers). |
| 36 | ✅ DONE (2026-09-12) | Compact row representation inside storage (Object[]) — CsvRowStorage/TsvRowStorage rows become List<Object[]> (slot i = schema column i value), sharing the exact arrays with DelimitedIndexManager via a package-private RowArrays (columns + case-insensitive index map, fromMap/toMap); Map materialisation only at the Map-based API boundary (scan/insert/insertAt/update/setRows/getBlock/parallel load), which also detaches stored rows from caller maps. CsvRowReader/TsvRowReader: new nextArray() fills Object[] directly; next()/readAll() wrap it (Map contract kept). CsvRowWriter/TsvRowWriter: new writeRow(Object[]) so saves stream arrays without Maps. AbstractRowStorage syncIndex* take Object[] + new syncIndexBulkFromArrays; SerializedTableData.rows re-typed to List<?> — pre-switch snapshots (Map elements, format v1) still deserialise via convertSerializedRows in both storages. New StorageArrayRepresentationTest (11 tests) incl. @LargeTest measurements (150k rows, 6 cols): retained heap 83.7MB→32.2MB (2.6x realistic unique-string rows), 58.3MB→6.6MB (8.8x JVM-cached-value rows) meeting the ≥3x per-row container-overhead acceptance criterion; array load 297ms vs map 198ms. Quick 966/0/0/6, full suite (4GB heap, @LargeTest) 196/0/0/0 BUILD SUCCESS, timing no heavy-query regressions. |
| 38 | ✅ DONE (2026-09-13) | CSV/TSV advanced negative-scenario + property tests — new CsvStorageAdvancedTest + TsvStorageAdvancedTest (18 tests each): header-by-name mapping (reorder / extra → single WARNING + extras ignored, missing → fail+rollback naming file & [COL], or warn → NULL), BOM/case-insensitive/escaped header columns, \N sentinel vs empty vs whitespace round-trip (TSV literal \N too, embedded CR safely escaped), file:line:column diagnostics + rollback, skip_row/skip_value modes, atomic-write crash safety (previous file intact, .tmp cleaned) + orphan-.tmp WARNING, broken-.table auto_mtime fallback, clustered insertAt + PK search, 2k-row bulk-delete index consistency, parallel==sequential 10k rows, 150-row random difficult-value round-trips, strict booleans + invalid rejection, malformed UTF-8 Cyrillic (KOI8-R / windows-1251) failures; registered in pom.xml (default + ci). Quick 233/0/0/6, -Ptest 1003/0/0/6, full 233/0/0/0 BUILD SUCCESS, timing exit 0. |
| 39 | ✅ DONE (2026-09-13) | CSV/TSV file compression — new CompressionCodec (interface) + CompressionFactory (resolver: none|zstd|lz4|snappy via forName, ZSTD level clamp 1-22 default 3, suffix detection for transparent reads, REPORT-decoder reader matching Files.newBufferedReader, nonClosing wrap for atomic commit); CsvRowStorage/TsvRowStorage write `<table>.<csv|tsv>.<suffix>` through AtomicFileWriter binary channel (compressor frame + commit), plain none path byte-identical; DelimitedIndexManager sequential/parallel codec-aware with parallel→sequential fallback for compressed (byte-offset pre-scan needs plain files). New config keys csv|tsv.compression.codec|level (sysprop → config.properties → default zstd; user default none in config.properties). pom.xml: zstd-jni/lz4-java/snappy-java + CompressionTest in default & ci includes. New CompressionTest (15 tests): all-codec round-trips, none byte-exactness, transparent reads both directions, suffix-wins-over-{stale-plain,old-codec}, csv/tsv independence, special chars/multiline/\N via compression, parallel fallback, unknown codec rejected, level clamping, sysprop override, ≥3x size + timing. Size: zstd 19.3x / lz4 6.4x / snappy 6.5x on 3000 repetitive rows. Timing (20k rows): none W73.8/R127.9ms vs zstd W188.7/R62.5ms (compressed read 2x faster). Quick 248/0/0/6, full 248/0/0/0 BUILD SUCCESS, heavy (≥100ms) queries stable. |
| 40 | ✅ DONE (2026-09-13) | JSONL storage base implementation — new diesel/storage/JsonlRowStorage + JsonlRowReader/JsonlRowWriter (jackson-core 2.17.2 streaming-only, no databind): rows persist to `<table>.jsonl` (one JSON object per line, UTF-8, `\n`) via AtomicFileWriter (temp+rename — interrupted save never truncates), streaming line-by-line load (memory O(1) in file size) with transactional rollback; field→column mapping by name without a header (missing → NULL, unknown ignored, duplicate last-wins — each one WARNING per file), blank lines skipped + BOM stripped, non-object records rejected, nested Map/List captured as compact JSON text via copyCurrentStructure, number conversions 2^53-exact from raw token text mirroring CSV/TSV readers, NaN/Infinity rejected at write; no .table mirror (P50) / compression (P52) / append (P49) / index manager yet (no-op bulk/syncIndex hooks); `jsonl` type registered in StorageFactory + config.properties comment updated; pom.xml adds jackson-core + JsonlStorageTest (default & ci includes). New JsonlStorageTest (22 tests): typed round-trips via reader/writer + storage save/load, empty table, null vs "" distinct, special chars (tab/newline/backslash/quote/Cyrillic/CJK/emoji), non-finite floats rejected at write, nested JSON text columns, BOM, blank lines, invalid JSON with file:line context, non-object record, missing/unknown fields, JSON bools/numbers into typed columns, update/delete persistence, missing-file load noop, interrupted save keeps previous file intact + cleans .tmp, corrupt-file rollback, 100k-row streaming write+read, StorageFactory (jsonl → JsonlRowStorage, default stays in_memory), Database-level INSERT/SELECT with diesel.storage.type=jsonl (no Table special-casing). Quick 270/0/0, full suite (4GB, @LargeTest) 270/0/0/0 BUILD SUCCESS, compare-timing exit 0. |
| 41 | ✅ DONE (2026-09-13) | JSONL extended features (Section 1b, prompt 41) — new diesel/storage/JsonlSchemaManager: single schema owner (ordered columns, case-insensitive index, column-type map); read-side validateReadToken rejects only the silent-corruption token shapes (nested object/array → typed non-String column; JSON number → Boolean/LocalDate/LocalDateTime/UUID) with file:line:field diagnostics; write-side validateWriteValue rejects Map/List/array into typed columns and Float/Double/BigDecimal into integer columns with `record N: field 'X'`; JSON Path dot-notation base — resolveProjectionItem (exact column or longest-prefix dot path → ProjectionSlot(columnIndex, segments, key)) + extractPathValue (token-level walk, no DOM, raw-token leaves); <name>.schema.json sidecar write/read/verify (SchemaDescriptor/SchemaColumn, SCHEMA_FORMAT_VERSION=1). JsonlRowReader: validation wired into nextArray(); new setProjection(Collection) / nextProjected() / nextProjectedMap() / getProjectionItems() — non-requested fields skipped at token level via skipChildren (no capture, no conversion) with getParsedFieldCount()/getSkippedFieldCount() counters; projection leaves full reads (next()/readAll()/nextArray()) untouched; unresolved projection items dropped with one WARNING; dot-path projection items extract nested leaves from captured STRING columns. JsonlRowWriter: new JsonlRowWriter(Writer, columns, columnTypes) 3-arg ctor enabling write validation (existing 2-arg ctor unchanged, validation off); JsonlRowStorage.saveToFile passes columnTypes to the writer (1-line wiring). New JsonlSchemaProjectionTest (19 tests): read validation coordinates (nested object/array in AGE/SESSION_ID, number in BIRTHDATE/ACTIVE, boolean in AGE — error carries file + line + field + token kind; JSON null allowed; string coercion stays lenient); write validation (nested in AGE fails record 1, float in AGE fails, BigDecimal in ID fails record 2, LocalDate in ID fails, int in ACTIVE fails; nested into STRING DATA allowed, key order independent); unknown field skipped+counted; projection returns only requested columns/output order/counters (3-of-40 cols: parsed=3x rows, skipped=37x rows), projection map keyed by item, projection doesn't affect full reads, dot-path DATA.user.address.city → "Moscow" with plain DATA keeping full JSON text + absent path → null, unresolved items dropped, clearing projection restores full rows; sidecar round-trip/replace-missing/type-mismatch/future-format detection; storage save-reject keeps previous file byte-identical (no stray .tmp); storage load-reject rolls back previous rows. Benchmark (20k rows x 40 cols, in-memory StringReader): full nextArray 620.8ms vs projected nextProjected 227.2ms — 2.7x faster, exact parity on values. Quick 297/0/0, full suite (4GB, @LargeTest) 297/0/0/0 BUILD SUCCESS, compare-timing exit 0 (only 2 heavy ≥100ms queries matched baseline, both stable). |
| 42 | DONE (2026-09-13) | JSONL streaming abstraction (Section 1b, prompt 42) - new diesel/storage/json/ package: JsonStreamParser, JsonStreamGenerator, JsonCopy, JsonStreams facade, JsonParserConfig (Backend JACKSON|GSON, maxNestingDepth=64, maxStringLength=1MB, duplicate-key FAIL|LAST_WINS), JsonStreamException; Jackson impls (JacksonStreamParser/JacksonStreamGenerator/JsonStreamsFactory) + Gson impls (GsonStreamParser strict + manual limits, GsonStreamGenerator byte-identical output). JsonlRowReader/JsonlRowWriter/JsonlSchemaManager migrated off direct Jackson imports (no JSON-lib import below the abstraction, bytecode-scan guard test). pom.xml: gson 2.8.9 + JsonStreamAbstractionTest in surefire includes. New JsonStreamAbstractionTest (14 tests): parser/copy/writer behavior equal across both backends and equal to the pre-abstraction Jackson implementation, byte-identical writer output, non-finite rejection, duplicate-key modes, depth + string limits. Quick 311/0/0/6, full suite (4GB, @LargeTest) 311/0/0/0 BUILD SUCCESS, compare-timing exit 0. |
| 53 | ✅ DONE (2026-09-15) | JSONL indexing: stable row-ids and JsonlIndexManager (Section 1b, prompt 53) — new public diesel/storage/JsonlIndexManager: in-memory index manager for JSONL rows held as Object[] (P36), fulfilling the P25 contract for the JSONL backend. Monotonic long rowIds (never reused across delete/restart, nextRowId persisted), TreeMap<Long,Integer> rowIdToPosition (positions shift on insertAt/update while rowIds stay stable), KEY_ORDER copied from DelimitedIndexManager; delete() marks tombstones (position shifts, auto-compaction over jsonl.compaction.threshold=0.3), compact()/reindex() reassign sequential 0..n-1; createIndex(String) adds secondary indexes (exact column or JsonPathResolver dot-path nested segment), getIndexColumns()/searchByPrimaryKey()/search()/rangeSearch() return physical positions; begin/endBulkUpdate() window. Persisted as a spirit sidecar <table>.idx via AtomicFileWriter.openBinary + ObjectOutputStream: serializable IndexSidecar (formatVersion=1, PK/secondary/nested trees, rowIdToPosition, nextRowId, stamp = dataPath + mtime/size + schema columns); syncFromDisk() adopts only when the stamp matches AND row count equals current rows, else rebuilds + recreates indexedColumns; corrupt/truncated sidecar → WARN + rebuild, never fails the load. JsonlRowStorage wiring: lazy jsonlIndex() routed through the protected syncIndex* hooks and beginBulkUpdate()/endBulkUpdate() (base class routes to the null delimited manager — the overrides are required), setPrimaryKeyColumn() override, private syncIndexAfterLoad() (no such hook exists in AbstractRowStorage) rebuilding + re-keying 0..n-1 after all three load paths, persistIndexAfterSave() at the end of saveRewriteMode and saveAppendMode; public getJsonlIndexManager()/getIndexColumns()/searchByPrimaryKey()/search()/rangeSearch(). Database.deleteTableFiles() also removes ".idx"/".idx.tmp" on DROP TABLE. New JsonlIndexTest (21 tests, registered in pom.xml default + ci): stable monotonic rowIds across append/middle-insert/update/delete, no PK re-keying on shifts, tombstone + auto-compact threshold, bulk-delete window with rewrite saves (tail-delete list stays linear), nested dot-path indexes surviving incremental shifts, sidecar round-trip/adopt-vs-rebuild, corrupt sidecar tolerated, unknown vs prefix-resolvable paths. Verification: quick 469/0/0/7, full suite (4GB, @LargeTest) 469/0/0/0 BUILD SUCCESS, JsonlIndexTest 21/21; compare-timing exit 0 (script's awk mis-parses the markdown rows, so the ≥100ms set was compared directly: complex-subquery 194.34→172.80ms, inner-join-on-PK 169.32→16.56ms — no heavy regression). Profile check skipped (no JOIN/hash join/performance wording); make/changelog/timing targets unavailable on this machine, gate ran as raw Maven commands and entry appended manually. |

### Section 2: Sonar Top-10 Pareto (41-50)

| ??? | ??T�-T?T?T? | ??-???-?-?-???? | ??T????-T???T�?T? |
|---|--------|----------|-----------|
| 41 | ??? DONE (2026-08-19) | ??-?-???-?- '[A-Za-z0-9_]' ?-?- '\w' ?- regex (S6353, 119 ??T??-?-?????-) | HIGH |
| 42 | ??? DONE (2026-08-19) | ??TH-??T�-T????-?? ?-??T�-?+?-?- T? ?-T?T??-???-?? Cognitive Complexity (S3776, 92 ??T??-?-?????-T?) | CRITICAL |
| 43 | ??? DONE (2026-08-19) | Pattern matching ?+??T? instanceof (S6201, 55 ??T????-?-T??-???-?-?-?-????) | MEDIUM |
| 44 | ??? DONE (2026-08-19) | ??T?T?T??-?-???-???? T?????T?T?T????-?-T?T? ???-T?T�?T??-?-?- ?- regex (S5998, 57 ??T??-?-?????-) + fix parseRightPart bug | HIGH |
| 45 | ? DONE (2026-08-19) | ?????????? ????????????? ????????? ????????? ? ErrorMessages (S1192) | MEDIUM |
| 46 | ? DONE (2026-08-19) | ???????? ?????????????? ???????? (S1128, 13 issues) | LOW |
| 47 | ? DONE (2026-08-19) | ??????????? break/continue ? ?????? (S135) | LOW |
| 48 | ? DONE (2026-08-19) | ???????? ?????????????? ?????????? ??????? (S1172, 10 params) | MEDIUM |

| 49 | ? DONE (2026-08-19) | ?????????? ?????? ?????? ???? (S108, 25 blocks) | HIGH |
| 50 | ? DONE (2026-08-19) | Deprecated setScale() (S1874) ? audit, 0 issues | MEDIUM |
### Section 3: Performance Optimizations (41-60)

| ??? | ??T�-T?T?T? | ??-???-?-?-???? | ??T????-T???T�?T? |
|---|--------|----------|-----------|
| 41 |? DONE (2026-08-19)| updateIndicesAfterInsert O(n+?m+?log n) | HIGH |
| 42 |? DONE (2026-08-19)| Nested Loop ??? Hash Join | HIGH |
| 43 | ???? IN_PROGRESS | ??-?+????T?T? ???-T????? DELETE | MEDIUM |
| 44 | ???? IN_PROGRESS | ????-T?T�?T??????-?-?-?-?-T?? ???-?+????T? T??-???+?-?-???? | MEDIUM |
| 45 | ???? IN_PROGRESS | ??-????T�-?-T? ?-T?T�-?-???- ??T??? ???-??T?T?????? | MEDIUM |
| 46 | ???? IN_PROGRESS | ??-?+????T?T? ?+??T? WHERE T?T????-?-???? | MEDIUM |
| 47 | ? DONE (2026-08-23) | ??-T?T??-?-T?? UPDATE | MEDIUM |
| 48 | ? DONE (2026-08-23) | indexDefinitions T???T????-???????-T??T? | MEDIUM |
| 49 | ???? IN_PROGRESS | ??-T?T�-T?-???? ???-?+????T??-?- ?- T???T????-???????-T???? | MEDIUM |
| 50 | ???? IN_PROGRESS | Copy-on-Write ?+??T? T?T??-?-???-??T???? | HIGH |
| 51 | ???? IN_PROGRESS | ??-T??-????????T?-?-?? ?-T???-???-???-???? ???-??T??-T??-?- | LOW |
| 52 | ???? IN_PROGRESS | ??T????-T?T??-?-?-T?? I/O | LOW |
| 53 | DONE (2026-09-01) | Compression ??T? T???T�? (GZIP negotiation + metrics) | LOW |
| 54 | DONE (2026-09-02) | Prepared Statements caching (implemented and fully tested as part of prompt 79) | MEDIUM |
| 55 | ???? IN_PROGRESS | Batch execution | MEDIUM |
| 56 | ???? IN_PROGRESS | Pagination T?????T??T?T�-T�-?- | MEDIUM |
| 57 | ???? IN_PROGRESS | Adaptive query execution | LOW |
| 58 | ???? IN_PROGRESS | Index-only scans | LOW |
| 59 |  DONE (2026-08-23) | Serialized index persistence | LOW |
| 60 | DONE (2026-08-23) | Copy-on-Write transaction isolation | LOW |

### Section 4: Parquet Integration (61-92)

| ??? | ??T�-T?T?T? | ??-???-?-?-???? | ??T????-T???T�?T? |
|---|--------|----------|-----------|
| 61 | ???? IN_PROGRESS | Apache Parquet ???-T�???T??-T??T? | HIGH |
| 62 | DONE (2026-08-23) | HIGH | QueryParser.java, SelectQuery.java | Cognitive Complexity S3776 refactoring | HIGH |
| 63 | ???? IN_PROGRESS | Columnar storage | HIGH |
| 64 | DONE (2026-08-23) | CRITICAL | QueryParser.java, SubqueryParser.java, SelectQuery.java | Recursive regex patterns S5998 | CRITICAL |
| 65 | DONE (2026-08-23) | CRITICAL | SqlKeywords.java, QueryParser.java, SqlLexer.java, SubqueryParser.java, Database.java | Extract repeated string literals S1192 | CRITICAL |
| 66 | ✅ DONE (2026-09-19) | BZip2 codec for cold/archival data (block size config + storage tiering) | MEDIUM |
| 67 | DONE (2026-08-23) | Remove unused method parameters S1172 | MEDIUM |
| 68 | DONE (2026-08-24) | Reduce break/continue in loops S135 | MINOR |
| 69 | DONE (2026-08-24) | Fill/remove empty code blocks S108 | MEDIUM |
| 70 | DONE (2026-08-24) | Remove deprecated setScale() S1874 | MEDIUM |
| 71 | DONE (2026-08-28) | Remove unused local variables (S1481) | MINOR |
| 72 | DONE (2026-08-28) | Remove useless assignments (S1854) | MEDIUM |
| 73 | DONE (2026-08-29) | ??-?-?-?????+?-T??T? ??T??? UPDATE | MEDIUM |
| 74 | DONE (2026-08-29) | ??-?-?-?????+?-T??T? ??T??? DELETE | MEDIUM |
| 75 | ??? DONE (2026-08-29) | ??-?-?-?????+?-T??T? ??T??? DDL | MEDIUM |
| 76 | ??? DONE (2026-08-31) | Parallel execution of independent queries | LOW |
| 77 | ???? IN_PROGRESS | ??T?T�?T??-?-?-?-???? Parquet | HIGH |
| 78 | ???? IN_PROGRESS | ??T?T�?T??-?-?-?-???? QueryCache | MEDIUM |
| 79 | DONE (2026-09-02) | Prepared Statements caching | HIGH |
| 80 | DONE (2026-09-02) | Batch execution support (BEGIN BATCH ... END BATCH) | MEDIUM |
| 81 | ???? IN_PROGRESS | ??-?-TH???T?T??-T??T? Parquet ?-?- T?T??-?-?-?? T�-?-????T?T? | MEDIUM |
| 82 | ✅ DONE (2026-09-03) | MEDIUM | QueryOptimizer.java, SelectQuery.java, QueryOptimizerTest.java | Adaptive query execution (monitor estimated vs actual rows, replan joins on deviation, LRU plan cache with fingerprint normalization) - verified: QueryOptimizerTest 10/10 green, quick gate 42/0/0/2 BUILD SUCCESS |
| 83 | ✅ DONE (2026-09-03) | Index-only scans | MEDIUM | SelectQuery.java, QueryProfiler.java, Database.java, ExplainQuery.java, CoveringIndexTest.java | Added index-only scan metrics: lastIndexLookupCount/lastIndexOnlyScanCount counters in SelectQuery, exposed via EXPLAIN ANALYZE and QueryProfiler JMX MBean, with tests - quick gate 42/0 BUILD SUCCESS |
| 84 | ✅ DONE (2026-09-03) | Parallel index scan | LOW | BTreeIndex.java, SelectQuery.java, config.properties, ParallelIndexScanTest.java | Added parallel index scanning: BTreeIndex gains rangeSearchParallel/rangeSearchLowParallel/rangeSearchHighParallel using a dedicated ForkJoinPool that splits work by root subtrees (subtreeMayContainInRange), estimates result size (countAllKeys/countKeysAbove/countKeysBelow/estimateBoundedRangeSize) with a configurable parallel.index.scan.threshold (default 10000) below which sequential scan is used; SelectQuery.lookupBTreeRange now calls the parallel variants; new ParallelIndexScanTest (8 tests) verifies parallel results match sequential for 20k-row indexes, boundary values, null bounds, empty ranges, and ascending order. Quick gate 42/0/0/0 BUILD SUCCESS (incl. ParallelIndexScanTest 8/8) |
| 85 | DONE (2026-09-04) | SIMD vectorization for aggregates | LOW |
| 86 | ???? IN_PROGRESS | Database.java ?+??T? Parquet default | MEDIUM |
| 87 | ???? IN_PROGRESS | ??-T??-?-?-T�??- ?-T???-?-?? ?-????T??-T???? | MEDIUM |
| 88 | ???? IN_PROGRESS | Partitioned tables ?- Parquet | MEDIUM |
| 89 | ???? IN_PROGRESS | Dictionary encoding ?+??T? T?T?T??-?? | LOW |
| 90 | ???? IN_PROGRESS | Compression tuning (ZSTD) | LOW |
| 91 | ???? IN_PROGRESS | Row group size tuning | LOW |
| 92 | ???? IN_PROGRESS | Column statistics metadata | LOW |

### Section 5: Advanced Features (93-100)

| ??? | ??T�-T?T?T? | ??-???-?-?-???? | ??T????-T???T�?T? |
|---|--------|----------|-----------|
| 93 | ???? IN_PROGRESS | Bloom filters ?+??T? Parquet | MEDIUM |
| 94 | ???? IN_PROGRESS | Cache warm-up strategy | LOW |
| 95 | ???? IN_PROGRESS | Adaptive TTL ?+??T? ??T?T?- | LOW |
| 96 | ???? IN_PROGRESS | Query normalization improvements | MEDIUM |
| 97 | ???? IN_PROGRESS | Parameterized query caching | MEDIUM |
| 98 | ???? IN_PROGRESS | Multi-level cache (L1/L2) | LOW |
| 99 | ???? IN_PROGRESS | Cache persistence across restarts | LOW |
| 100 | ???? IN_PROGRESS | Final integration testing & docs | HIGH |

## Legend

- ???? IN_PROGRESS - Not started
- ???? IN_PROGRESS - Currently working
- ??? DONE - Completed
- ?????? BLOCKED - Blocked by dependency

## How to Update

1. Choose next prompt from Priority Queue (top of table)
2. Change status to ???? IN_PROGRESS
3. After implementation and tests pass, change to ??? DONE
4. Add date completed in format `??? DONE (2025-01-15)`


